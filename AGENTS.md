# Astarray — Agent 工程指引

本仓库由 OpenCode 按 `IMPLEMENTATION_PLAN.md` 分批实现。开始编码前必须读取：

- `agent-main-architecture.md`
- `designtodo.txt`
- `IMPLEMENTATION_PLAN.md`
- `PLAN_STATUS.md`

## 常用命令

```powershell
npm run typecheck   # tsc --noEmit
npm run lint        # eslint .
npm run test        # vitest run
npm run test:coverage
npm run build       # tsup
npm run check       # typecheck + lint + test + build
npm pack            # 打包前自动运行 prepack(check)
```

## 强制规范

- 生产代码变量/函数名必须含义完整，禁止无语义缩写（见实施计划 §3）。
- 布尔变量使用 `is`/`has`/`can`/`should` 前缀。
- 时间量命名必须带单位（如 `deliveryDelaySeconds`）。
- 核心领域代码（`packages/core/src/core`）禁止使用 `any`。
- 权限在工具实际执行前检查，不只派发时检查。
- 协同模式的任意安装尝试（包括代码库、项目依赖、运行时、插件、工具链、系统包及会执行安装脚本或改变依赖解析结果的操作）必须执行两阶段门禁：先询问用户是否已有可用资源；确认没有后，仅当独立安装功能开关已开启，才可发起绑定精确内容和参数的逐次用户授权。开关不得代替授权，不得使用会话记忆、Agent 推断或批量许可放行。
- 权限配置使用逐项三态决策 `deny/ask/allow`。放权模式的全部可配置权限默认 `allow`，协同模式使用独立默认矩阵，思索模式不可调整。用户可创建不设产品数量上限、具有唯一 ID 和可辨识名称的自定义权限模式并逐项配置。底层强制约束不进入可配置权限目录，不生成设置按钮，也不得由 TUI/CLI、帮助、导出配置、普通错误详情或模型提示向用户披露其存在或规则清单。
- 主 Agent 模型在任何内置、自定义或临时提升后的模式中始终只能调用读取类工具；创建次级 Agent 和权限组控制属于本地会话控制面，不是主 Agent 工具。权限组及当前会话临时提升只决定会话内次级 Agent 的权限上限；次级 Agent 分发给三级 Agent 的权限必须是其当前有效权限的等于或更窄子集。
- 当前会话允许认证用户临时提升次级 Agent 的公开权限；默认作用于该会话全部现有及后续次级 Agent，也可显式限定具体次级 `agentInstanceId`。提升绑定会话、作用域、权限项、有效期和 revision，可随时撤销，关闭会话即失效且不修改基础 profile。关闭会话时可导出会话级公开有效权限为配置文件或新自定义模式；个体覆盖可选择分别导出，但不得导出授权 nonce、Agent 身份、会话令牌、一次性许可或内部执行信息。
- 思索模式只允许本地只读白名单工具查看项目文件、检索文本和查询只读任务状态；编辑、进程执行、网络访问、备份访问及任何未知或副作用不明确的工具必须由本地 harness 直接禁用，不能仅靠提示词约束。
- 工具副作用与敏感操作分类必须由本地确定性策略、参数检查和操作系统边界完成；云端模型或 AI 判断只能提供提示，不能成为授权依据。
- `.env`、`.env.*`、私钥、凭据库和本地敏感路径在思索、协同、放权三种模式下都禁止模型读取；该禁令由本地路径/内容策略在打开或返回内容前执行，模式升级、会话授权和模型声明均不能绕过。
- 高严谨性任务由本地规则标记，并强制调用事实验证工具形成证据包。证据优先级固定为“资料搜索依据 > 本地实验验证 > 纯推理验证”；工具只能报告来源、覆盖、冲突与局限，不能替用户给出最终合格结论。
- 文件读取和其他可重复工具必须使用本地反自指/活锁保护：同一调用源短时间重复读取未变化资源时拒绝并返回已有读取回执和剩余等待时间；循环调用、无进展重试和 Agent 互相回派必须有本地检测与有界中止。
- 反馈工具为独立进程，不得退化为进程内定时器或协程。
- 每条反馈消息必须携带经过 schema 校验的信息来源；Agent 来源必须具体到不可复用的 `agentInstanceId`，转发时保留原始来源。
- 主、次级、三级每个具体 Agent 都以不可复用的 `agentInstanceId` 为唯一存档所有者，使用独立记忆域、工作存档、上下文、缓存和消息视图；禁止角色级或同级共享存档。同级 Agent 之间也不得直接读写对方记忆。跨 Agent 仅可经本地控制器显式选择条目，生成带来源、revision 和哈希的不可变附件；附件只用于当前任务且不得自动并入接收方长期记忆。
- 涉及 Git 写入的多 Agent 任务必须由次级 Agent 负责分支/工作树分流、差异审查、测试验证和受控合并；三级 Agent 只能提交到分配给自己的隔离分支，不得自行合并、变基、推送或删除分支。
- 每个调度 Agent 必须在自己的记忆存档域维护独立的待办任务偏序集；任务发布者可指定插入前驱/后继。用户任务默认优先级层级 0，Agent 或工具生成的任务只能使用层级 1 或更低优先级；调度从当前可执行节点中按优先级、同级稳定顺序选取。
- 默认控制流中，主 Agent 只与用户交流、评估并生成任务插入提案；本地控制面验证来源和 revision 后把原始用户指导或主 Agent 派生设计插入次级 Agent 的待办偏序集。用户来源保持层级 0，主 Agent 派生节点仍是 Agent 来源且只能层级 1 或以下。主 Agent 提交后立即继续对话，不等待后台执行。
- 次级 Agent 持续从自己的 ready set 按偏序和优先级向三级 Agent 分发任务链，决定复用还是新建具体三级个体，并负责三级 Agent 生命周期、Git 分流/审查/合并，以及在当前有效权限允许时执行 GitHub/远端项目控制和传输。三级 Agent 一次激活只处理一条任务链，完成或中断后及时向任务发布源汇报，不得自行领取链外任务。
- 次级 Agent 的任务终态汇报只由本地接收器写入主 Agent 报告索引，不得自动唤醒主 Agent 模型或注入当前对话；主 Agent 仅在后续用户交流需要时只读选择相关报告。三级 Agent 成功、终止、上下文/消息预算超限或健康异常时，由次级 Agent 通过受控工具收口并关闭；需要接手时创建新 `agentInstanceId`，显式选择旧存档条目形成 handoff，禁止复用身份或默认复制完整历史。
- 次级和三级 Agent 第一次接收某工具组时获得完整公开用法；同一工具组 revision 后续只注入固定的 `ASTARRAY_TOOL_HELP_REQUEST_V1` 提醒。Agent 忘记用法或缺少工具时必须按标准格式请求；当前已分配工具由本地控制器直接返回该工具用法，未分配、不可用或不存在的能力按默认上级逐级反馈。三级 Agent 的默认工具帮助上级始终是其所属次级 Agent。工具说明回执按具体 `agentInstanceId` 隔离，不构成授权。
- 各层 Agent 实例不设累计、同级或存档数量的产品硬上限；单会话仍只有一个当前用户沟通主 Agent。并发、Provider、内存和磁盘限制只用于排队、暂停与回收，不得转化为历史实例数量配额。
- 直属上级经当前权限和用户裁决授权后，可把直属低一级 Agent 的不透明沟通句柄转交给一个具体同级 Agent。转交只允许独立反馈进程中的限定通信，不暴露 IPC 地址或令牌，不转移任务所有权、记忆、偏序集、Git 职责、工具分配或权限；grant 不可再转授，Agent 回收、关系/任务/profile revision 变化、到期或撤销时失效。
- 任何删除、文本删减、替换、截断或覆盖必须由执行工具在变更前自动备份；自动备份过程不经过模型。后续只能经受控备份工具读取或恢复。
- 删除备份使用独立特权入口：协同模式必须警告、暂停发起 Agent 并逐次取得用户授权；放权模式不提示但写入高查阅优先级审计日志。
- 模式中文名称固定为：Ponder＝思索模式、Assist＝协同模式、Devolve＝放权模式。
- 每次模型任务完成时必须返回版本化的明确完成控制事件；本地运行时只有在事件合法且任务状态、验收门禁和未决调用均确认完成后才结案。输出提前结束、缺失完成事件或标识与本地状态不符时，由本地看门狗从检查点有界续跑，禁止无限重试或重复副作用。
- 打包验收必须以 tarball 隔离安装为准。
- 主 Agent 仍是单会话唯一的连续用户对话者，负责总体方向、全面布局、跨任务权衡和把技术反馈转写为适合用户理解水平的说明。默认消息发送给主 Agent；目标明确且无需方案讨论的小任务可由界面推荐“直接交由次级执行”，认证用户确认后由本地控制面把原始用户任务信封直投具体次级 `agentInstanceId`，但不建立用户与次级 Agent 的独立对话。次级结果默认以 `SECONDARY_USER_FACING_SUMMARY_V1` 汇报主 Agent，再由主 Agent告知用户；原始来源和关键风险不得在转述时丢失。
- 次级 Agent 是项目执行细节所有者。需要了解项目时优先派出只读 `project-reconnaissance` 三级 Agent 查阅允许范围内的源码、文档、测试和 Git 信息，并只接收带来源、revision、hash、内容指纹和 token 预算的 `PROJECT_CONTEXT_DIGEST_V1`；大量原文与工具输出保留在侦察 Agent 个体存档，不得灌入次级上下文。
- 次级 Agent 负责任务详细规划、偏序集插入、分发、三级生命周期和项目级 Git 集成，并为代码任务任命具体实现、测试和验收三级 Agent。实现者不得验收自己的产出；高风险任务的实现、测试、验收必须使用三个不同 `agentInstanceId`。次级只有在提交哈希、测试、验收、人工体验和操作授权门禁均满足时才能判定 merge-ready，否则必须返修或等待用户。
- 三级 Agent 在紧急、任务量大、存在可并行子链或用户策略允许时，可经本地控制面向四级 Agent 分发自己任务链内的严格子链。四级 Agent 使用独立不可复用身份、记忆、mailbox、缓存、存档和 worktree，权限/工具/范围/期限不得宽于三级，且不能继续创建第五级。三级可审查并把直属四级贡献合入自己获分配的三级任务分支，但仍不得向 mission 集成分支、用户目标分支或远端合并；项目级最终集成仍只由次级 Agent 执行。
- 四级 Agent 的工具说明回访沿用 `ASTARRAY_TOOL_HELP_REQUEST_V1`：首次接收工具组时获得完整公开用法，同 revision 后续只收固定提醒；其默认工具帮助上级是所属三级 Agent，未分配或不可用能力再由三级逐级上报。工具说明回执仍按四级具体 `agentInstanceId` 隔离且不构成授权。
- 主、次级、三级、四级每个具体 Agent 均可在用户允许的模型/Provider 列表内独立选择或在安全检查点切换模型；模型切换不得改变 `agentInstanceId`、记忆域、任务所有权、工具、权限、Git 职责或优先级。用户可按层级、用途和具体任务类型设置不设产品数量上限的模型列表，并创建办公文档、前端、编码、debug、测试、验收、侦察、绘图等 `TaskAgentPreset`。固定列表是硬上限，列表耗尽时阻塞而不是使用未授权模型；Provider 凭据不得进入模型上下文、日志、导出或反馈。
- 内置“工匠”是休眠的三级 Agent预设 `tertiary-preset:craftsman-v1`，只用所属次级已能分发的现有已授权基础工具定制可复用工作流。会话开始时不得创建实例或向次级 Agent注入完整说明；本地确定性阶段控制器根据活跃会话时长、已验收任务链、版本化里程碑、项目记忆公开索引规模或重复工作流指纹，在策略命中后经独立反馈进程向具体次级 Agent发送 `CRAFTSMAN_PRESET_AVAILABLE_V1`。用户可配置不设数量上限的阶段模板、`any/all` 阈值、提示词和披露动作；自动提示节点只能使用优先级层级 1 或以下，且不构成安装、权限、合并或验收授权。工匠产物必须生成 `CRAFTSMAN_WORKFLOW_BUNDLE_V1` 并由不同 Agent测试/验收。
