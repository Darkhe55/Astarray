# Astarray 验收问题修复任务书

> 状态：待执行  
> 编制日期：2026-08-12  
> 执行主体：OpenCode 及其受控次级 Agent  
> 依据：`agent-main-architecture.md`、`designtodo.txt`、`IMPLEMENTATION_PLAN.md`、`PLAN_STATUS.md` 及本轮验收结果

## 1. 目标

在不改变既定产品方向的前提下，修复本轮验收发现的安全边界、身份一致性、备份并发、精确恢复和测试覆盖问题，使项目重新满足以下发布条件：

- 普通文件工具无法读取、列出、修改或删除备份保管库及备份删除审计数据。
- 协同模式下删除备份会真实暂停对应 Agent，并使用正确的保管库版本完成逐次用户授权。
- 独立反馈进程能够验证反馈来源，Agent 来源不可由消息正文冒充。
- 调度、反馈、工作存档和任务状态中使用同一个不可复用的 `agentInstanceId`。
- Agent 工作存档路径编码对任意 Unicode 标识符无碰撞，且只能显式选择存档条目附加给下级 Agent。
- 备份保管库、审计哈希链和恢复流程在并发、崩溃及文件/目录类型切换场景下保持一致。
- 所有删除、删减、替换、截断和覆盖继续遵守“执行工具在变更前自动备份，模型不参与备份”的强制规则。
- npm tarball 可在隔离目录安装并运行，完整质量门禁通过。

## 2. 当前验收基线

开始修复前，应复现并记录基线，不得用本文件替代实际检查：

- `npm run check` 当前通过。
- 当前测试基线为 32 个测试文件、412 个测试。
- 当前覆盖率约为：语句 93.11%、分支 85.77%、函数 89.57%、行 93.22%。
- npm tarball 隔离安装冒烟测试当前通过。
- 最终架构验收未通过，原因见第 5 节任务。

上述数字是回归下限，不是修复后的目标。不得通过删除测试、降低阈值、排除关键文件或扩大 `/* istanbul ignore */` 来维持数字。

## 3. OpenCode 执行协议

### 3.1 开始前必读

每个新会话和每个新执行 Agent 在编码前必须完整读取：

1. `AGENTS.md`
2. `agent-main-architecture.md`
3. `designtodo.txt`
4. `IMPLEMENTATION_PLAN.md`
5. `PLAN_STATUS.md`
6. 本文件 `AUDIT_REMEDIATION_TASKS.md`

### 3.2 一次完成多少任务

- 每次只执行一个 `AR-*` 主任务。
- 一个主任务可包含多个不可分割的子步骤，但同一轮最多修改 8 个生产代码文件；测试和文档文件不计入此上限。
- 涉及权限、身份、备份删除、恢复事务或跨进程协议的任务属于高风险任务；每轮只能完成一个高风险任务，不得与其他主任务合并。
- 如果一个主任务预计超过 3 小时，必须在保持构建可通过的边界处分拆，不得通过延长单次等待规避拆分。
- 单次等待或轮询参数 `pn` 的硬上限为 3 小时。正常轮询建议 10 秒至 60 秒；长任务必须持续输出进度，不得静默等待 3 小时。
- 不得同时让两个 Agent 修改相同文件。可并行执行只读分析或互不相交的测试编写，但合并前由上级 Agent 检查冲突。
- 涉及 Git 写入的并行任务由次级 Agent 创建独立分支/worktree，并作为唯一集成者完成差异审查、测试和合并；三级 Agent 禁止自行合入集成/目标分支。
- Agent 待办任务保存在其记忆存档域中的独立偏序集；发布者可指定前驱/后继插入位置。用户任务默认优先级层级 0，Agent、system 或工具生成任务只能层级 1 或以下。

推荐批次顺序：

| 批次 | 主任务 | 风险 | 前置任务 |
|---|---|---:|---|
| 0 | AR-00 基线与证据冻结 | 低 | 无 |
| 1 | AR-01 受保护存储边界 | 高 | AR-00 |
| 2 | AR-02 协同模式授权与真实暂停 | 高 | AR-01 |
| 3 | AR-03 反馈来源认证 | 高 | AR-02 |
| 4 | AR-04 Agent 身份与工作存档一致性 | 高 | AR-03 |
| 5 | AR-05 备份并发与精确恢复 | 高 | AR-01、AR-02 |
| 6 | AR-06 全局破坏性操作收口 | 高 | AR-05 |
| 6A | AR-06A Ponder 本地只读边界 | 高 | AR-01、AR-06 |
| 6C | AR-06C 全模式敏感内容禁读 | 高 | AR-01、AR-06A |
| 6D | AR-06D 反自指与通用活锁守卫 | 高 | AR-04、AR-06C |
| 6E | AR-06E 高严谨性事实验证 | 高 | AR-06C、AR-06D |
| 6B | AR-06B 完成协议与早停恢复 | 高 | AR-03、AR-04、AR-06D、AR-06E |
| 6F | AR-06F Assist 安装前置询问与逐次授权 | 高 | AR-02、AR-06、AR-06C、AR-06B |
| 6G | AR-06G 可配置权限组与自定义模式 | 高 | AR-02、AR-06C、AR-06F |
| 6H | AR-06H 主 Agent 只读与会话临时提升 | 高 | AR-03、AR-04、AR-06G |
| 6I | AR-06I 默认控制流、个体记忆隔离与三级生命周期 | 高 | AR-03、AR-04、AR-06H |
| 6J | AR-06J 工具说明回访与受权 Agent 通信转交 | 高 | AR-03、AR-06G、AR-06I |
| 7 | AR-07 覆盖率、打包和文档终验 | 中 | AR-01 至 AR-06、AR-06A 至 AR-06J |

### 3.3 变量命名

- 使用完整、可读的英文变量名和函数名，禁止 `tmp`、`ctx`、`mgr`、`req`、`res`、`cfg`、`rev` 等无语义缩写。
- 布尔量使用 `is`、`has`、`can` 或 `should` 前缀。
- 时间量必须包含单位，例如 `authorizationTimeoutMilliseconds`、`deliveryDelaySeconds`。
- 身份相关变量明确区分 `agentLogicalRole`、`agentInstanceId`、`missionId` 和 `taskId`。
- 路径变量明确区分 `workspaceRelativePath`、`canonicalAbsolutePath`、`protectedStorageRootPath`。

### 3.4 Agent 工作存档

- 每个次级、三级 Agent 使用自己的工作存档文件，文件所有者由不可复用的 `agentInstanceId` 标识。
- 存档条目至少记录：任务、结论、证据、未决问题、创建时间、修订号和来源 Agent。
- 上级 Agent 创建下级任务或重新调用下级 Agent 时，默认不附加任何历史存档。
- 只有上级 Agent 明确选中的条目才可以附加；必须记录选择者、条目 ID、条目修订号、选择原因和目标 Agent。
- 禁止“开启布尔开关后自动注入最近全部存档”的实现。

### 3.5 每个主任务的完成流程

1. 读取前置文档和目标代码，写出不超过十行的本任务计划。
2. 检查 `git status --short`，不得覆盖用户未提交的相关修改。
3. 先补失败测试或安全反例，再修改生产代码。
4. 运行目标测试、类型检查和 lint。
5. 运行 `npm run check`。
6. 更新本任务的证据记录；只有全部完成条件满足后，才可更新 `PLAN_STATUS.md`。
7. 输出修改文件、测试结果、剩余风险和下一主任务，不得宣称未验证事项已完成。

## 4. 统一设计约束

### 4.1 模式名称与行为

| 英文模式 | 中文名称 | 删除备份行为 |
|---|---|---|
| Ponder | 思索模式 | 禁止删除备份，不进入授权流程 |
| Assist | 协同模式 | 警告、暂停发起 Agent、逐次用户授权、授权后才继续 |
| Devolve | 放权模式 | 不弹出提醒，写入高查阅优先级审计日志后执行 |

不得出现其他中文译名。

### 4.2 权限检查时机

- 派发阶段可以预检，但工具执行前必须再次检查。
- 检查使用规范化后的实际目标路径和当前模式，不得只信任模型给出的字符串。
- 打开文件后、写入前如有 TOCTOU 风险，应重新验证文件身份或内容指纹。

### 4.3 自动备份边界

- 任何删除、删减、替换、截断或覆盖在执行前自动创建备份。
- 备份由执行工具完成，备份凭证、对象哈希和物理存储路径不得进入模型上下文。
- 普通工具不得访问备份保管库；读取、恢复和删除备份分别使用受控工具入口。
- 删除备份自身不得递归创建备份，必须通过独立特权入口和审计规则避免死锁。

## 5. 修复任务

## AR-00：基线与证据冻结

### 问题

`PLAN_STATUS.md` 中部分“已修复”结论与本轮动态验收不一致。如果不先冻结基线，后续可能把原有问题和新回归混在一起。

### 实施步骤

1. 记录 Node、npm、操作系统和 Git 提交信息。
2. 运行：

   ```powershell
   npm run check
   npm run test:coverage
   npm pack --dry-run
   npm pack
   ```

3. 使用生成的 tarball 在全新临时目录安装，验证：

   ```powershell
   astarray --version
   astarray --help
   astarray doctor
   astarray run --mock
   astarray-feedback-process
   ```

4. 保存失败与通过证据，创建修复前状态表。不要在此任务中修改生产代码。
5. 将 `PLAN_STATUS.md` 中受本文件影响的结论标记为“重新验收中”，不得继续显示为最终通过。

### 测试与完成条件

- 基线命令输出、tarball 文件名和退出码可追溯。
- 工作区原有未提交修改已识别并保留。
- 没有把环境问题误记为代码修复。

## AR-01：隔离备份保管库与审计存储

### 当前问题

当前 `WorkspaceBoundary` 只验证路径是否位于工作区内。默认状态目录 `.astarray` 也位于工作区，因此普通 `readFile`、`listDirectory`、替换和删除工具可能直接访问：

- `.astarray/backup-vault/**`
- `.astarray/backup-deletion-audit.jsonl`
- 后续新增的保管库锁、暂存和隔离目录

这会绕过受控备份读取、恢复和删除入口。

### 设计方案

新增统一的 `ProtectedStoragePolicy`，将“工作区边界”和“受保护存储边界”分开处理。

建议接口：

```ts
interface ProtectedStoragePolicy {
  assertGenericToolAccessAllowed(input: {
    canonicalTargetPath: string;
    operation: "read" | "list" | "create" | "replace" | "delete";
  }): Promise<void>;
}
```

实现要求：

1. 在普通工具实际执行文件操作前调用策略，不得只在注册或派发阶段检查。
2. 对已存在路径使用 `realpath`；对不存在路径解析最近的已存在父目录，防止符号链接逃逸。
3. 路径判断使用规范化路径组件，不使用字符串前缀比较。
4. 普通工具访问受保护根或其子路径时统一拒绝。
5. 普通工具列出受保护根的父目录时过滤受保护条目，避免暴露物理布局。
6. 备份内部服务使用进程内能力对象进入保管库，不提供可由模型构造的绕过参数。
7. `listBackups` 返回公开 DTO，只包含逻辑目标、备份 ID、时间、状态和工具名；不得返回对象哈希、恢复能力标识或物理路径。
8. `readBackup` 对二进制内容返回显式编码和媒体类型，不得无条件按 UTF-8 损坏内容。

### 预计涉及文件

- `packages/core/src/tools/workspace-boundary.ts`
- `packages/core/src/tools/builtins.ts`
- `packages/core/src/backup/backup-vault.ts`
- 新增 `packages/core/src/tools/protected-storage-policy.ts`
- 对应单元测试和集成测试

实际文件名可以按仓库结构调整，但职责不得重新混入 UI 层。

### 必测场景

- 普通读取、列目录、替换、删除保管库文件全部拒绝。
- 使用 `..`、大小写变化、Windows 分隔符、目录联接和符号链接无法绕过。
- 列出 `.astarray` 不返回受保护子项。
- 受控 `listBackups`、`readBackup` 和 `restoreBackup` 仍可使用。
- 公开备份列表不含哈希、能力标识和物理路径。
- 权限预检后目标被替换为链接时，执行前复检能够拦截。

### 完成条件

- 受保护存储只能由备份服务和特权删除入口访问。
- 安全反例测试在修复前失败、修复后通过。
- `npm run check` 通过。

## AR-02：修复协同模式授权版本并真实暂停 Agent

### 当前问题

`packages/tui/src/cli/backup-deletion-port.ts` 当前对允许和拒绝都返回固定的 `expectedVaultRevision: 1`。当保管库修订号大于 1 时，即使用户输入允许，控制器也会拒绝操作。

同时，当前实现主要显示警告，没有可靠地把发起 Agent 切换到 `awaiting-user-authorization` 并阻止该 Agent 继续调用工具。

### 设计方案

1. 扩展 `BackupDeletionAuthorizationRequest`，至少包含：

   ```ts
   interface BackupDeletionAuthorizationRequest {
     authorizationRequestId: string;
     requestingAgentInstanceId: string;
     backupIdentifier: string;
     expectedVaultRevision: number;
     authorizationExpiresAtIso: string;
     warningMessage: string;
   }
   ```

2. 授权端口只能原样回显请求中的 `authorizationRequestId`、`expectedVaultRevision` 和到期时间，不得生成魔法数字或自行延长有效期。
3. 在核心层新增 Agent 执行闸门，例如 `AgentExecutionAuthorizationGate`：
   - 进入授权前将指定 Agent 状态设置为 `awaiting-user-authorization`。
   - 暂停该 Agent 的新工具调用和任务推进。
   - 不暂停无关 Agent、独立反馈进程和用户控制通道。
   - 允许、拒绝、超时或异常后均在 `finally` 中解除等待状态。
4. 发起授权前必须释放文件锁和调度锁，禁止持锁等待用户，避免死锁。
5. 协同模式必须显示风险、目标备份、发起 Agent 个体和不可逆后果，并逐次取得授权。
6. 思索模式直接拒绝；放权模式不询问，但写入高查阅优先级、哈希链保护的审计记录。
7. 非交互终端不得假装获得授权，应安全拒绝并恢复 Agent 状态。
8. TUI 使用受控对话框或控制通道；避免直接 `readline` 与 Ink 原始输入模式争抢标准输入。

### 必测场景

- 保管库修订号为 2、10 和更大值时，正确授权可以成功。
- 旧修订、错误请求 ID、过期授权和重复授权全部拒绝。
- 状态序列包含 `running -> awaiting-user-authorization -> running/idle`。
- 等待期间同一 Agent 的新工具调用被阻塞，无关 Agent 和反馈进程继续运行。
- 用户拒绝、终端关闭、超时和授权端口抛错后不会永久暂停。
- 授权期间不持有保管库锁。
- 放权模式无提示但存在高优先级审计；思索模式无删除。

### 完成条件

- 删除备份不存在固定修订号。
- 协同模式的暂停是可观察、可测试的真实状态，不只是 UI 文案。
- 每份授权只能消费一次且只适用于一个备份和一个 Agent 个体。
- `npm run check` 通过。

## AR-03：建立反馈来源认证和不可伪造的发送通道

### 当前问题

反馈消息已有来源 schema，但来源字段仍来自消息载荷。父进程可先用任意 ID 注册状态，再用该 ID 发送消息，因此“字段合法”不等于“来源真实”。

### 设计方案

反馈进程继续保持独立进程。将身份注册和发送能力从消息内容中分离：

1. 上级编排器创建 Agent 实例时，通过可信控制通道注册 `agentInstanceId`、角色和生命周期。
2. 注册成功后生成不可由模型读取或构造的进程内发送能力；能力令牌不得写入提示、工具结果、普通日志或工作存档。
3. Agent 发送接口不接受自由填写的 Agent 来源。来源由已绑定的发送能力自动注入。
4. IPC 信封携带注册句柄或短期能力证明，独立反馈进程校验后再进入队列。
5. `setAgentStatus` 只能更新已注册实例，不能隐式注册新身份。
6. Agent 生命周期结束时撤销发送能力；重新派发使用新的 `agentInstanceId` 和能力。
7. 用户来源只能由用户控制通道注入，系统来源只能由受信任的编排器控制通道注入。
8. 转发消息同时保存：
   - 原始来源 `originalInformationSource`
   - 当前转发者 `forwardingAgentInstanceId`
   - 转发链 `provenanceChain`
9. 每一跳验证 schema、实例身份、顺序和链长度上限，禁止转发时把原始来源改为转发者。
10. 断线重连不得允许旧能力绑定到新实例。

### IPC 协议建议

将控制消息和数据消息使用判别联合类型分开：

```ts
type FeedbackProcessCommand =
  | RegisterAgentIdentityCommand
  | RevokeAgentIdentityCommand
  | UpdateRegisteredAgentStatusCommand
  | EnqueueAuthenticatedFeedbackCommand
  | AcknowledgeFeedbackCommand;
```

所有命令先做运行时 schema 校验，再做能力和生命周期校验。不得用 TypeScript 类型断言替代运行时验证。

### 必测场景

- 未注册 Agent、伪造 Agent ID、错用其他 Agent 能力全部拒绝。
- 已撤销能力、旧进程能力和重连前能力全部拒绝。
- 用户、系统和 Agent 三类来源不能互相冒充。
- 次级和三级 Agent 的来源明确到具体 `agentInstanceId`。
- 转发保留原始来源，并记录每个转发 Agent。
- 非法 IPC、超长链、损坏消息、重复消息和乱序确认不会使独立进程崩溃。
- 反馈进程退出后由监督器按策略重启，未确认消息按持久队列规则恢复。

### 完成条件

- 模型可控字段不能决定 Agent 身份。
- 独立反馈进程仍能单独启动、停止、重连和测试。
- `npm run check` 和反馈进程隔离测试通过。

## AR-04：统一 Agent 实例身份并修复工作存档路径编码

### 当前问题

当前调度器可能记录 `worker:<mission>:<task>`，实际启动时又生成 `worker:<mission>:<task>:<counter>`。这会导致任务图、反馈来源和工作存档记录不同的 Agent ID。计数器在进程重启后还可能复用。

现有路径转义还存在碰撞：字面量 `~003a` 可能与冒号的转义结果相同；使用 `charCodeAt` 处理 emoji 会丢失低代理项。

此外，存档附加必须是明确条目选择，不能由一个布尔开关自动加入最近全部内容。

### 设计方案

#### A. 单一身份分配

1. 新增 `AgentInstanceIdentityFactory`，使用 `randomUUID()` 或同等级随机标识生成不可复用 ID。
2. 可读前缀只用于诊断，例如：

   ```text
   worker:<mission-short-id>:<task-short-id>:<uuid>
   ```

   唯一性由完整 UUID 保证，不由内存计数器保证。
3. 在任务进入 running 状态前分配一次 ID，并将同一值写入任务图。
4. `MissionOrchestrator` 启动 Agent、注册反馈身份和创建工作存档时，只能使用任务图中的该值，不得二次生成。
5. 任务重新分配默认生成新实例 ID；只有恢复仍然存活且可证明为同一进程的实例时才可复用。

#### B. 无碰撞路径编码

1. 使用 UTF-8 字节的 base64url 编码，例如 `id-<base64url>`。
2. 编码函数输入原始 ID，返回带品牌类型的 `EncodedPathSegment`；调用方只能编码一次。
3. 提供严格解码函数并验证重新编码一致性。
4. 删除“任意字符串编码必须幂等”的要求。对任意原始字符串同时要求可逆、无碰撞和自动识别已编码值会产生歧义，应在类型边界上区分原始值和编码值。
5. 读取旧存档时可做一次受控迁移：
   - 优先读取新路径。
   - 旧路径只在文档内部 ID 与请求 ID 完全匹配时接受。
   - 发现旧路径碰撞时停止并报告，不得覆盖。
   - 迁移涉及替换或删除时必须先自动备份。

#### C. 显式选择工作存档

派发数据应携带显式选择项，而不是布尔值：

```ts
interface WorkArchiveSelection {
  archiveOwnerAgentInstanceId: string;
  archiveRevision: number;
  selectedEntryIdentifiers: string[];
  selectionReason: string;
  selectedByAgentInstanceId: string;
}
```

- 默认值为空数组。
- 只加载所列条目，并验证所有者、修订号和任务范围。
- 选择记录进入任务审计，但不得泄露未选择内容。
- 工作存档写入应使用绑定到具体 Agent 的 writer capability，避免任意 Agent 写入他人存档。

#### D. 次级 Agent Git 集成职责

1. 为每个 mission 固定目标基线，并由具体次级 `agentInstanceId` 创建和独占集成分支。
2. 每个三级 Agent 获得绑定任务、身份、基线和允许路径的独立分支/worktree；只能提交到自己的分支。
3. 次级 Agent 验证提交祖先、身份来源、修改范围、敏感信息、命名、自动备份合规和测试证据后才可合并。
4. 审查失败通过独立反馈进程下发修正；冲突创建独立修复任务，不得静默选择一侧内容。
5. 合并后运行目标测试和 `npm run check`，生成结构化 Git 集成报告，再按模式和用户授权决定是否合入目标分支。
6. Git push、PR、发布、强制推送和受保护分支修改不自动授权。
7. Git 历史不能替代自动备份；破坏性 Git 操作必须由底层工具预先创建受保护恢复点。

#### E. Agent 待办任务偏序集与任务包

1. 每个调度 Agent 使用独立、版本化的任务序列文件，存放在其记忆存档域，不写入项目 `task-chain.json`，不复制项目产出。
2. 任务序列以 DAG 表示偏序。发布者添加节点时可以指定直接前驱、直接后继或二者；不指定时保持不可比，插入后必须仍然无环。
3. 用户来源任务默认 `priorityTier: 0`；Agent、system 和工具来源只能层级 1 或以下。来源和优先层在执行层认证，模型不能伪造。
   次级 Agent 分割用户任务时保留层级 0 的用户根节点；派生任务仍为层级 1 或以下，并作为根节点的必要前驱。
4. 调度器先计算 ready set，再按优先层和稳定同级顺序选取。用户任务不抢占进行中的原子调用，但优先占用下一执行槽。
5. 高优先任务的必要低层前驱可以先行，并记录可由状态工具解释的依赖原因。
6. 次级 Agent 可将适合同一三级 Agent 的一条真实链冻结成任务包；包内保留逐节点状态，失败/阻塞节点禁止其后继执行。
7. 控制面 `taskSequenceStatus` 只读工具允许发布者在任意模式随时查询序列、任务、ready set、顺序解释和任务包，不打断 Agent，不混入项目产出；Ponder 模型只能在认证可见范围和本地只读白名单内调用。
8. 插入、删边、改序、取消和清理任务历史属于可能破坏既有信息的变更，执行工具必须先自动备份。

### 必测场景

- 任务图、运行实例、反馈来源和存档所有者使用完全相同的 ID。
- 重启后重新分配不会复用旧 ID。
- 冒号与字面量 `~003a`、emoji、中文、空格、Windows 保留字符均无碰撞且可往返。
- 非法 base64url、超长 ID 和路径穿越输入被拒绝。
- 默认不附加存档；只附加明确选择的条目。
- 旧修订、他人条目、跨任务条目和被删除条目不能静默附加。
- 次级和三级 Agent 分别写入自己的存档，不能写入兄弟 Agent 的文件。
- 两个三级 Agent 在隔离 worktree 并行工作，不共享索引或未提交状态。
- 三级 Agent 无法合并、变基、推送、删除分支或写入集成分支。
- 越界路径、错误基线、伪造来源和缺少测试证据的提交被次级 Agent 拒绝。
- 最终集成提交可追溯到具体三级 Agent、任务、贡献提交和审查证据。
- 任意合法插入保持任务序列无环；非法环、未知锚点、陈旧 revision 和越权改序均拒绝。
- 用户任务默认层级 0；Agent/system/工具请求层级 0 被执行层硬拒绝。
- ready set 内严格按优先级执行，同级顺序确定且可重放；必要低层前驱具有可解释原因。
- 一条任务链可打包给具体三级 Agent，节点失败会阻止包内后继。
- 状态查询在 Agent busy 时仍可用且无副作用；Agent 序列不包含或覆盖项目任务与产出数据。

### 完成条件

- 删除所有第二套 Agent ID 生成逻辑和可复用内存计数器。
- 路径编码对全 Unicode 输入无碰撞。
- 存档附加具备逐条、可审计的显式选择语义。
- 次级 Agent 是 Git 分流、审查和合并的唯一责任人；所有贡献有结构化集成报告。
- Agent 待办任务以独立偏序集持久化，发布者可插入、用户优先层不可被自动任务冒用，并提供无副作用状态工具。
- `npm run check` 通过。

## AR-05：增加保管库并发控制和精确恢复事务

### 当前问题

保管库清单采用内存对象加原子写入，但缺少覆盖同一路径的统一互斥。多个异步操作或多个实例可能发生修订丢失、后写覆盖先写。备份删除审计的“读末尾哈希再追加”也可能并发分叉。

目录备份只保存文件内容时，无法保证恢复空目录、删除快照后新增文件，或精确还原文件与目录类型。

### 设计方案

#### A. 保管库事务和锁

1. 所有保管库变更通过同一个事务入口执行。
2. 同一进程内按规范化保管库路径共享互斥锁，不能每个实例各有一把无关的锁。
3. 若允许多进程访问，增加跨进程租约锁或等效机制，包含所有者 nonce、进程信息、创建时间、到期时间和有界等待。
4. 获得锁后重新从磁盘读取并校验清单，再验证预期修订、构造新清单并原子提交。
5. 提交完成并按需要 `fsync` 后才更新内存缓存。
6. 锁等待必须有超时和诊断；不得持锁等待用户授权、反馈确认或网络操作。
7. 崩溃恢复只能接管可证明已过期的租约，不能仅依据 PID。
8. 临时事务文件、锁文件和隔离文件的生命周期必须纳入 AR-06 的破坏性操作清单，禁止形成绕过备份规则的隐藏删除路径。

#### B. 审计哈希链串行化

1. 在同一个审计锁内完成读取最后哈希、生成新记录和追加。
2. 每条记录包含单调序号、前一条哈希、当前记录哈希、时间、模式、发起 Agent 和授权信息。
3. 启动时验证整条链；链损坏时禁止继续删除备份，并给出高优先级告警。
4. 并发追加测试必须证明只有一条线性链，不允许两个记录引用同一前序哈希。

#### C. 快照格式

升级快照 schema，明确记录：

- 目标原始状态：`missing`、`file` 或 `directory`。
- 文件的相对路径、字节内容哈希、长度和必要元数据。
- 所有目录条目，包括空目录。
- schema 版本和完整快照哈希。
- 对符号链接采用明确策略：默认拒绝并报告，除非架构文档另行批准。

不得把“不存在”目标保存成空文件，否则恢复无法回到原始缺失状态。

#### D. 精确恢复

1. 恢复前验证备份状态、能力、清单修订和对象完整性。
2. 若当前目标存在，先由工具自动备份当前目标，并验证 TOCTOU 指纹。
3. 在同一文件系统的唯一暂存路径完整构建恢复结果。
4. 验证暂存结果的文件数、目录数、长度和哈希。
5. 通过可回滚交换替换目标：
   - 先将现有目标移入事务隔离位置。
   - 将暂存结果原子移动到目标位置。
   - 提交失败时将原目标移回。
6. 目录恢复必须移除快照后新增的额外文件，恢复空目录，并支持文件与目录之间的类型切换。
7. 原始状态为 `missing` 时，恢复结果必须为目标不存在；相关删除仍需走受控恢复事务并保留前置备份证据。
8. 恢复成功、回滚成功和回滚失败分别写结构化审计。

### 必测场景

- 50 个并发创建、恢复、隔离请求不丢修订、不覆盖条目。
- 两个 `BackupVault` 实例指向同一目录时结果仍线性一致。
- 并发审计追加形成单一哈希链。
- 在清单写入前、对象写入后、交换中途和审计追加时注入崩溃，可恢复到明确状态。
- 恢复目录会删除额外文件并重建空目录。
- `missing -> file -> restore` 最终回到缺失；文件与目录可互换恢复。
- 二进制文件逐字节一致。
- 保管库对象损坏、清单损坏和审计链损坏均安全失败。
- Windows 上打开文件、重命名冲突和防病毒短暂占用有有界重试，不无限等待。

### 完成条件

- 保管库变更具有可证明的线性化点。
- 审计链在并发下不分叉。
- 恢复结果与快照完全一致，而非只覆盖同名文件。
- `npm run check` 通过。

## AR-06：收口所有破坏性文件操作

### 当前问题

即使普通工具已经接入备份，配置原子写、状态持久化、doctor 探针、打包验证脚本、迁移脚本或临时目录清理仍可能直接调用 `rm`、`unlink`、截断式 `writeFile` 或覆盖式 `rename`。这些路径需要系统性盘点，不能依赖代码评审记忆。

### 设计方案

1. 搜索并分类全部破坏性调用，包括但不限于：

   ```powershell
   rg -n "rm\(|rmSync\(|unlink\(|unlinkSync\(|truncate\(|writeFile\(|rename\(|copyFile\(" packages scripts
   ```

2. 建立 `DestructiveMutationGuard` 或等效统一入口。业务目标发生删除、删减、替换、截断或覆盖时，必须在底层执行器中先备份。
3. 上层调用只提交意图和目标，不能传入“跳过备份”布尔值。
4. 内部状态原子提交需要明确设计：
   - 业务内容仍必须进入备份保管库。
   - 日志追加不得变成全文件覆盖。
   - 事务暂存文件必须使用不可预测名称和排他创建。
   - 如确需清理内部事务产物，必须由受保护存储服务执行并形成专用审计，不能成为模型可调用的通用删除后门。
5. `doctor` 优先使用不产生删除的权限检查；如果必须创建探针，探针的创建、所有权和清理规则要被测试和审计。
6. 打包验证使用唯一临时目录，不得先递归删除固定目录。
7. 禁止 shell 命令、第三方适配器或 MCP 工具绕开统一执行入口。
8. 加入静态架构测试或 ESLint 限制：除白名单底层模块外，生产代码不得直接调用破坏性文件 API。
9. 白名单必须逐项说明理由，不接受整个目录或 `scripts/**` 无条件放行。
10. 将 Git 的 reset、clean、checkout 覆盖、rebase、强制移动引用、删除分支/worktree 和冲突处理纳入破坏性操作清单；执行前自动保存未提交内容 pre-image、引用和恢复元数据。

### 必测场景

- 每种生产工具的删除、替换、文本删减、截断和覆盖都会先产生可恢复备份。
- 纯新建且目标确实不存在时不会制造无意义的旧内容备份。
- 并发下“检查不存在后新建”使用排他创建，不能覆盖竞态创建的文件。
- 配置迁移、状态保存、doctor 和打包验证没有固定路径递归清理。
- 静态检查能够阻止新代码在非白名单位置直接使用破坏性 API。
- 三级 Agent 不能绕开次级 Agent 集成器直接执行破坏性 Git 命令；次级 Agent 执行时仍需自动恢复点和审计。
- 删除备份只存在一个特权入口，不因统一 guard 递归备份自己。

### 完成条件

- 形成完整的破坏性调用清单和逐项处置结果。
- 不存在模型可控的跳过备份参数。
- 静态架构测试、目标测试和 `npm run check` 通过。

## AR-06A：Ponder 本地只读边界与本地敏感操作检测

### 当前问题

旧实现和测试把 Ponder 设计成全部工具拒绝，无法基于真实项目文件进行可靠思索；若直接放开 `readonly` 类别，又可能被伪造 descriptor、通用 shell、写入标志、路径别名或云端误判绕过。敏感操作判断若依赖模型或 Provider，安全边界会随模型变化并在断网时失效。

### 设计方案

1. 实现 `LocalToolPolicyEngine`，在工具 schema 暴露和实际执行前分别检查当前模式、认证主体、稳定工具 ID、能力声明、参数和资源范围。
2. Ponder 白名单仅包含专用本地只读适配器：项目文件读取、目录列举、文本检索、`taskSequenceStatus` 和固定 Git 只读查询。
3. 通用 shell、任意进程、直接/通用网络、连接器、文件/Git 写入、安装、发布、凭据、`backupVault`、`deleteBackup` 不向 Ponder 暴露；高严谨性资料查询只经隔离事实搜索代理，其他构造调用仍由 wrapper 硬拒绝。
4. 实现 `LocalSensitiveOperationClassifier`，在 Ponder/Assist/Devolve 策略判断前统一用版本化静态规则和确定性参数检查分类文件/Git 变更、进程、网络、外部副作用、凭据、备份和系统操作。Provider 或 AI 的判断不得产生 allow 决策。
5. 本地路径策略检查规范化绝对路径、realpath、符号链接/联接点、文件打开标志及紧邻 IO 的复检；受保护状态、备份物理存储和凭据路径始终排除。
6. Ponder 模型运行单元在 OS 层移除写入、进程执行和直接网络能力；事实搜索代理使用独立最小网络沙箱。策略 wrapper 和 OS 沙箱形成两个独立拒绝点，未知或无法证明只读时 fail-closed。
7. 拒绝事件只记录规则版本、稳定工具 ID 和脱敏原因；Ponder 不写项目、任务、记忆、缓存或遥测状态。

### 必测场景

- 普通工作区文件读取、目录列举、文本检索、获授权任务查询和固定 Git `status/diff/log` 成功。
- 写入标志、创建、删除、替换、截断、重命名、Git 写入、shell、任意网络、安装、发布、凭据与备份工具失败；只有结构化事实查询可以进入隔离代理。
- `../`、绝对/UNC 路径、大小写、符号链接/联接点、TOCTOU、包装命令、别名、伪造 `readonly` descriptor 和未知工具均不能绕过。
- Assist 授权后降级 Ponder，下一调用仍本地拒绝；旧授权不继承。
- 无网络、Provider 不可用以及 AI 给出错误安全分类时，授权矩阵完全相同。
- 同一敏感调用在 Ponder 硬拒绝、Assist ask/deny、Devolve 强制系统边界/审计的结果均由本地规则决定。
- 调用前后工作树、任务状态和 Ponder 状态文件集合无变化；关键分支覆盖率不低于 95%。

### 完成条件

- ADR-0014 的全部允许/拒绝规则有动态证据，不只依赖 schema 快照。
- 本地策略与 OS 只读边界均有独立失败测试和集成测试。
- `npm run check` 通过；本任务单独提交、单独验收。

## AR-06C：全模式本地敏感内容禁读

### 设计方案

1. 在模式鉴权之前增加 `SensitiveContentAccessPolicy`，固定拒绝 `.env`/`.env.*`、私钥、凭据库、能力令牌和管理员敏感路径；本版本没有授权例外。
2. 路径身份同时检查 lexical path、realpath、符号链接/联接点、硬链接文件身份、平台大小写、Git 对象路径和归档成员。
3. 对名称正常的工具结果使用本地可信 DLP 流式扫描；命中或扫描失败时丢弃整个模型可见结果，不能回传部分内容。
4. read/list/search、Git、归档、OCR/解码、shell/插件/MCP、事实验证上传、缓存、日志、错误、反馈和存档全部接入同一策略。
5. 程序自身配置装载位于最小权限隔离层，只向 Provider 客户端提供不回显句柄/值，不向模型或普通工具暴露原始环境变量。

### 必测场景

- 三种模式、用户明确要求、会话授权和 Devolve 均无法读取 `.env`、`.env.local`、`service.env`、SSH/PGP 私钥及凭据库。
- `.ENV`/`.Env.Local` 等大小写变体、相对/绝对/UNC、链接/联接点、硬链接、Git history、archive、base64/编码、重命名和插件/MCP 不能绕过。
- 列举和搜索不泄漏内容；错误、日志、证据、缓存、反馈和存档无秘密字节。
- 普通非敏感配置仍按原模式策略工作；程序运行所需凭据可被隔离客户端使用但不进入模型。

### 完成条件

- ADR-0018 的所有入口具有红灯反例和动态集成证据。
- 关键分支覆盖率不低于 95%，`npm run check` 通过；本任务单独提交和验收。

## AR-06D：反自指读取与通用活锁守卫

### 设计方案

1. 读取成功后按具体 Agent、任务、规范资源、操作、范围和参数保存不含正文的读取回执，默认未变化读取抑制窗口为 30 秒。
2. 重复请求先本地验证文件身份与内容指纹；未变化且范围已覆盖则返回 `resource-already-read`、回执和剩余间隔，不返回正文。
3. 文件/网页/工具输出/存档均为不受信数据，其中的调用文字不能直接生成工具调用；用户指定文件也不能覆盖权限或要求自我递归。
4. `LocalProgressAndCycleGuard` 检测自环、A→B→A、Agent 祖先回派/乒乓、搜索换词、范围切片、反馈回声和早停续跑旁路。
5. 相同在途调用 single-flight；无任务 revision、产物、有效证据或用户信息变化时累计无进展次数，默认 3 次暂停。include/redirect 设深度、节点、扇出和总预算。
6. 等待时不持锁；重启、换 Agent、改变无意义参数、touch 时间戳或等价内容抖动不能清零任务级预算。

### 必测场景

- 自指文件只产生一次正文读取；时间窗结束、内容真实变化或未覆盖新范围时可以重读。
- 路径别名、大小写、链接/硬链接、范围重叠和参数噪声不能绕过；不同 Agent/任务互不误伤。
- 工具/资源/Agent 环、搜索换词、重复错误、反馈重投和早停续跑在有限次数内停止并报告循环链。
- 长工具、真实新内容、有效新证据和用户补充不被误判；fake clock、并发、重启和锁超时可确定重放。

### 完成条件

- ADR-0017 的时间锁、指令数据隔离、single-flight 和循环图均有动态证据。
- 关键分支覆盖率不低于 95%，`npm run check` 通过；本任务单独提交和验收。

## AR-06E：高严谨性事实验证

### 设计方案

1. `LocalRigorPolicyEngine` 用本地规则标记高严谨性；法律、医疗、财务、安全、权限、破坏性操作、发布和时效事实默认 high。模型不能降级。
2. 提供 `factVerification` 的资料搜索、本地实验、纯推理登记和证据包构建动作；搜索经专用只读代理，不开放任意网络能力。
3. 证据按“资料搜索 > 本地实验 > 纯推理”组织。资料保存发布者、直接链接/文档标识、日期、取得时间和正文摘要；实验保存环境、步骤、状态和产物哈希；推理保存前提与不确定性。
4. `EvidenceCompletionGate` 检查工具调用、关键 claim 覆盖和来源正文。搜索不可用时可形成不足状态，但不能把纯推理提升为已证实。
5. 输出只表达 supported/contradicted/mixed/insufficient/unavailable、冲突和局限，不返回最终 qualified/safe/pass；TUI/CLI 明确等待用户判断。
6. 所有查询先经 ADR-0018 泄密门禁，并用规范化查询指纹、结果缓存、来源去重和预算抑制换词活锁。

### 必测场景

- 高严谨性缺少工具调用、正文来源或关键 claim 覆盖时不能完成；普通任务不强制联网。
- 伪造链接、搜索摘要、重复转载、过期资料和 Agent 自述不能冒充资料证据；矛盾资料完整展示。
- 离线、来源撤回和实验失败得到不足/不可用状态，不虚报合格；用户要求离线时无网络调用。
- `.env`、私钥、项目正文、备份和完整提示词不能被查询上传；来源提示注入不能触发工具。

### 完成条件

- ADR-0016 的严谨性判定、三层证据、证据门禁、用户裁决边界和离线语义均有动态证据。
- 默认 CI 使用假搜索代理；关键分支覆盖率不低于 95%，`npm run check` 通过；本任务单独提交和验收。

## AR-06B：明确完成协议与早停恢复

### 当前问题

当前运行时可能把 Provider 请求结束或自然语言“完成”视为任务结束。模型因长度、超时、断流或自身早停机制提前停下时，未完成的实现和测试不会继续；反过来，简单文本完成标识又可能从用户输入、项目文件或工具输出中被伪造。

### 设计方案

1. 定义 `TaskCompletionEventV1`，要求模型在真正完成时返回 `ASTARRAY_TASK_COMPLETION_V1` 控制事件，绑定任务执行 ID、一次性尝试 ID、完成节点、结果摘要和任务 revision；需要用户输入使用独立 `TaskBlockedEventV1`。
2. 优先使用结构化控制帧/受控协议调用。文本兼容模式只接受最终独立末行；普通正文、用户输入、文件和工具输出中的标识无效。
3. `LocalCompletionVerifier` 本地验证标识防重放、节点/前驱状态、产物、测试/构建门禁、事实证据门禁、循环预算、未决工具、权限请求、反馈确认和阻塞状态；全部通过后才原子写入一次 `done`。
4. `AgentRunWatchdog` 以带单位配置定期检测请求/进程活性、流式或心跳进展、工具状态、任务 revision、完成事件和结束原因。云端 `finish_reason` 只是证据，不能直接结案；无进展超时只触发健康探测。
5. 可继续的疑似早停先原子保存检查点，再生成新的尝试 ID 和未完成清单续跑。工具幂等键跨续跑保留；不确定的非幂等副作用进入 blocked，不得自动重做。
6. 默认 5 秒检查、90 秒无进展、5 秒完成事件宽限、最多 3 次自动续跑。活跃请求、运行中的长工具或授权等待不会启动并行请求。
7. 用户取消、需要输入、明确拒绝、不可恢复错误和不确定副作用分别进入明确终态；达到上限后经独立反馈进程发送带具体来源、检查点和缺口的失败消息。

### 必测场景

- 合法完成事件加完整本地验收只提交一次；重复事件和重启重放幂等。
- `stop`、`length`、连接中断、超时、进程退出、缺失事件和被拒绝的虚假完成声明触发检查点续跑，且最多 3 次。
- 错误任务 ID、错误尝试 ID、陈旧 revision、正文/用户/文件/工具输出中的伪造标识不能结案。
- 模型先声称完成但产物缺失、测试未跑、节点未完成、工具/权限/反馈未收口时继续缺口。
- 慢速流、长时间只读工具、授权等待、Provider 仍活跃时不会误判或发起第二请求。
- 旧请求停止状态不确定时进入 `blocked`；仅在确认结束或安全取消成功后续跑。
- 用户取消、`ASTARRAY_TASK_BLOCKED_V1`、明确拒绝、不可恢复错误和不确定写入不自动循环。
- 续跑上下文只包含可信检查点、未完成节点和按规则选择的存档，不泄漏敏感数据，不越权追加完整 Agent 历史。
- `ScriptedRuntime` 离线覆盖全部分支；关键分支覆盖率不低于 95%。

### 完成条件

- ADR-0015 的完成、拒绝、阻塞、早停、上限、重启和幂等路径均有动态测试证据。
- 对至少两类模拟 Provider 结束语义执行契约测试，默认 CI 无真实网络依赖。
- `npm run check` 通过；本任务单独提交、单独验收。

## AR-06F：Assist 安装前置询问、独立开关与逐次授权

### 当前问题

现有实现把安装工具默认排除，但没有完整表达未来开放安装时的用户控制边界。若只把安装作为普通 restricted 工具或只增加一个总开关，Agent 可能在用户已有代码库、依赖或工具链时重复安装，也可能复用十分钟会话授权执行来源、版本或作用域不同的后续安装。

### 设计方案

1. 实现 `InstallationOperationClassifier`，按实际副作用识别安装：代码库 clone/download/vendor、项目依赖新增或更新、运行时/编译器/浏览器、插件/技能/模型、工具链和系统包，以及生命周期安装脚本、lockfile/vendor 改写和依赖解析变化。不得仅匹配 `install` 字样。
2. 首次需要具体资源时，在任何下载或安装探测前发出 `existing-resource-inquiry`，列明用途、能力和兼容条件，并暂停具体 Agent。认证用户回答“已有”时，只允许选择资源并执行只读兼容性、版本、完整性和来源检查；不兼容时再次交给用户决定，不能自动转为安装。
3. 用户明确回答“没有可用资源”后，检查独立设置 `isAssistInstallationEnabled`。默认关闭；关闭时拒绝。开关只能由认证用户经 TUI/CLI 设置控制面修改，写入先自动备份并使用单调 revision。Agent、项目文件、插件、环境变量和模型无权修改。
4. 开关开启只允许提出 `assist-installation-request`，不允许直接安装。请求绑定具体用户、Agent、任务、来源、包/仓库、精确版本或 commit、完整性信息、目标、作用域、工具、参数、网络、安装脚本、lockfile 摘要和预计变化。
5. 用户授权仅为本次 `allow-once`。不得接入普通 Assist 十分钟会话授权，不得批量泛化、记忆、转授或由 Agent 代答。请求绑定字段、模式、开关 revision、计划或参数变化后授权立即失效。
6. 执行前本地复检认证身份、模式、开关 revision、一次性 nonce、参数哈希、来源与目标边界、敏感内容策略和自动备份状态。执行一次后原子消费授权；失败重试若会重新运行安装脚本或改变计划，必须再次询问。
7. 等待已有资源答复或安装授权时，释放文件锁、任务领取锁和包管理器锁。对应状态分别为 `awaiting-existing-resource-answer` 与 `awaiting-installation-authorization`，不阻塞无依赖 ready 任务。
8. Headless 非交互模式没有可信、请求绑定的用户确认通道时 fail-closed。`--yes`、包管理器自动确认、环境变量、模型文本和任务文档均不能替代用户授权。

### 必测场景

- npm/pnpm/yarn、pip/uv/poetry、cargo、git clone、系统包管理器、插件/技能安装、包装 shell 和间接脚本被一致分类。
- 开关默认关闭；开关开启但没有本次授权、只回答“没有”、只有普通会话授权或 Agent 声称用户已同意时，均没有网络、下载、文件、lockfile、缓存或脚本副作用。
- 每个安装需要先询问是否已有资源。用户提供兼容资源时复用且不安装；资源不兼容时暂停并显示差异，不自动下载替代品。
- 用户确认没有、开关开启并允许精确计划后只执行一次；重放、过期、拒绝、超时、参数/版本/commit/来源/目标/脚本变化、模式切换和开关 revision 变化全部拒绝。
- 批量计划中的每项均可见；授权后新增依赖不能搭便车。registry token、`.env` 和凭据不进入请求、审计、反馈或模型。
- 等待用户时不持锁；多个 Agent 同时请求安装不会共享答案、nonce 或授权，也不会死锁。
- TUI 设置开关、已有资源问答、安装详情、允许本次/拒绝/修改参数和暂停状态可见；无 TTY 时稳定 fail-closed。

### 完成条件

- ADR-0019 的两阶段门禁、默认关闭开关、逐次授权、执行前复检、授权消费、无锁等待和非交互拒绝均有动态证据。
- Windows、Linux 和可用时 macOS 覆盖代码库、依赖、插件及系统包代表场景；测试使用隔离假 registry/假安装器，不修改开发机全局环境。
- 关键分支覆盖率不低于 95%，`npm run check` 通过；本任务单独提交和验收。

## AR-06G：可配置权限组与无限命名自定义模式

### 当前问题

当前权限策略把 Ponder/Assist/Devolve 和工具 `readonly/restricted/forbidden` 类别写死，无法让用户逐项收紧 Devolve、为 Assist 保存另一套默认选择，或创建多个命名权限组合。TUI 还使用三项循环切换，任务、缓存和审计只保存三值模式，无法安全区分可重命名的自定义权限组及其 revision。

### 设计方案

1. 建立版本化 `PermissionCapabilityCatalog`，逐项列出项目读/建/改/破坏性修改、沙箱/宿主进程、网络读/写、浏览器读/交互、连接器读/写、数据库读/写、云资源读/写、剪贴板读/写、环境读/改、代码库取得、项目/全局依赖安装、扩展安装、Git 读/本地写/历史改写/远端写、发布、通信、资金、系统修改、备份 list/read/restore/delete、Agent spawn/delegate/manage/communication-delegate、任务 read/manage和选择性记忆读/记忆写。工具可映射多项权限，取最严格决定；未知权限或未映射工具 fail-closed。
2. 每项决定为 `deny/ask/allow`。Devolve 出厂 profile 的所有目录项均为 allow；Assist 使用独立默认矩阵并可逐项修改；Ponder profile 由程序签名冻结，所有编辑、导入和直接控制器调用均拒绝。
3. 实现 `PermissionProfileStore`，使用不可复用 ID、可变显示名、单调 revision、目录版本、原子持久化、并发冲突检测和自动备份。Assist/Devolve 修改互不影响，内置组可恢复出厂值但不可删除。
4. 用户可创建不设产品数量上限的自定义 profile。支持空白全 deny或复制任一内置/自定义组；名称经 Unicode 规范化和大小写折叠后唯一，禁止冒充内置中英文名。重命名不改变 ID，复制后完全独立；未来新权限使用每组 fallback，默认 deny。
5. 运行态使用 `builtin:<name>` 或 `custom:<permissionProfileId>`，任务、反馈、授权、缓存和审计绑定 ID、名称快照、revision 与目录版本。profile revision 或工具权限映射改变后，未执行调用、旧 ask 授权和旧缓存不得复用。
6. TUI/CLI（以及未来 GUI）设置共用同一认证控制器，提供 profile 选择、创建、命名、复制、重命名、删除、导入/导出、重置和逐项三态选择。Ponder 不进入权限编辑页；三模式循环快捷键改为可搜索选择器。大量 profile 使用分页/虚拟列表，不设数量配额。Agent 只能建议用户打开选择器，不能自动进入更宽权限组。
7. 权限组创建、命名、复制、导入/导出和删除只存在于认证用户设置控制面，不注册为 Agent 工具，也不受当前 profile 自我锁定；Agent 和模型不能调用或代替用户操作该控制面。
8. 内部执行层不进入 capability catalog、profile schema、按钮、禁用项、锁图标、帮助、权限详情、导出、普通用户审计或模型工具描述。命中只返回最小“操作不可用”结果，不泄露规则名称、类别、条件或绕过建议；详细分类只进隔离内部审计。
9. 导入只接受公开可配置 schema，忽略并拒绝任何内部字段。覆盖、重置、导入和删除先自动备份；当前使用中的自定义组删除前必须先选择另一个有效组。

### 必测场景

- Devolve 初始/重置后权限目录每项均为 allow；逐项切换 deny/ask/allow 后，schema 暴露、权限询问和实际执行保持一致。
- Assist 与 Devolve 同一权限可以取不同值且互不污染；Assist 默认矩阵符合 ADR-0020。安装权限即使为 allow 也仍经过 ADR-0019 的专用流程。
- Ponder 在 UI、CLI、导入、API、直接状态文件、迁移、别名和并发竞态下都不可改变，并且没有权限编辑按钮或“不可调整”占位项。
- 创建、命名、复制、重命名、选择和删除至少 10,000 个自定义组；代码不存在最大组数常量或按数量拒绝的分支。通用磁盘耗尽、超大单文档和并发冲突仍安全失败。
- Unicode 等价名称、大小写冲突、保留名、伪造内置 ID、路径穿越、重复 ID、陈旧 revision、损坏导入和未知权限被拒绝；重命名不改变已有任务绑定的 ID。
- 工具同时映射多个权限时严格取最严值；目录新增/升级/降级、自定义 fallback 和未知目录版本不扩大权限。
- 设置变更使旧授权、缓存和等待调用失效；并发更新不丢失，损坏 profile 恢复时不回退到更宽配置。
- 用户设置页、帮助、导出 JSON、普通日志/错误/审计和模型可见 schema 均只包含公开可配置权限，不出现内部执行层的名称、数量、规则清单或存在性提示。
- 创建/导入/重置/删除均验证自动备份；删除当前组不造成无有效模式状态。

### 完成条件

- ADR-0020 的权限目录、三态裁决、内置默认、自定义生命周期、无数量上限、ID/revision 绑定、迁移和产品面隔离均有动态证据。
- TUI 三种目标尺寸、键盘/鼠标路径、长名称、搜索、分页/虚拟列表和 Headless JSON 契约通过；Node 20 和支持的较新版本通过。
- 关键分支覆盖率不低于 95%，`npm run check` 通过；本任务单独提交和验收。

## AR-06H：主 Agent 永久只读、次级权限上限与会话临时提升

### 当前问题

现有主控制器会直接构造运行时和调度器，权限策略也没有区分主/次级/三级作用域。若把新 profile 直接套到所有 Agent，Devolve 全 allow 或会话临时提升会让主 Agent 获得执行工具；次级 Agent 也可能向三级 Agent 分发比自己更宽的权限。当前关闭流程还没有安全导出“有效公开权限”后销毁临时覆盖的事务顺序。

### 设计方案

1. 实现 `MainAgentReadonlyToolProjection`，主 Agent 在任意 profile、任意会话覆盖和任意模式下只得到读取类专用工具。主 Agent 模型工具表不得出现写入、进程、网络写入、安装、Agent spawn/manage、权限设置或配置导出。
2. 将“创建次级 Agent”移到认证本地 `SecondaryAgentSessionController`。主 Agent 只提交任务内容；控制器生成不可复用次级 `agentInstanceId`，绑定 session、profile ID/revision、目录版本和权限快照。模型字段不能声明身份或权限。
3. 建立 `SessionPermissionElevationStore/Controller`。认证用户可为当前 session 全部现有及后续次级 Agent 创建会话级 ask/allow 覆盖，也可为具体次级 Agent 创建个体覆盖；均可带资源范围和到期时间。每条记录绑定作用域、可选 Agent、基础 profile/revision、目录版本、会话权限 revision 和用户裁决引用，可查看、撤销和提前收窄。
4. `EffectiveSecondaryPermissionResolver` 计算基础 profile、会话级覆盖和具体 Agent 个体覆盖。会话级覆盖对前后创建的次级 Agent 一致生效，个体覆盖彼此隔离。会话关闭、到期、撤销、profile/revision/目录变化后覆盖失效，Agent 回收撤销个体覆盖，并使旧工具授权、缓存和未执行调用重鉴权。
5. `TertiaryPermissionDelegationGuard` 对次级向三级的 capability、三态、工具、任务类型、资源范围、目标路径和期限取交集。严格使用 `deny < ask < allow`；三级不能读取完整 profile、直接请求用户或向兄弟 Agent 转授。
6. 权限不足由三级经独立反馈进程上报所属次级，次级创建用户提升请求。主 Agent只在对话中转达，不拥有、消费或持久化提升。请求绑定 session、具体次级 Agent 和 capability。
7. 实现 `CurrentPermissionConfigurationExporter`，默认导出基础 profile 加会话级覆盖的当前会话公开有效权限；存在个体覆盖时可导出指定 Agent 的最终配置或分别导出。支持版本化 JSON 或创建新的命名自定义 profile，字段只含公开 capability、资源范围、目录版本、来源 profile 和显示元数据。
8. 导出执行白名单序列化，禁止 session ID、Agent ID、nonce、裁决签名、令牌、普通会话授权、一次性安装/备份许可、到期计时器、内部审计/执行字段。导入后的配置没有原会话授权能力。
9. `SessionShutdownCoordinator` 顺序为：停止新派发 → 收敛/取消在途调用 → 可选导出 → 无条件撤销所有临时覆盖 → 关闭会话。跳过、取消、导出失败或崩溃恢复都不能延长覆盖；覆盖已有导出文件先自动备份。
10. TUI/CLI/未来 GUI 共用控制器，分别展示明确标注的会话级提升和具体次级 Agent 个体提升、基础 profile、有效公开权限、到期和撤销。禁止“提升主 Agent”；Headless 只有显式导出参数才写文件。

### 必测场景

- 使用 Assist、Devolve、全部 allow 自定义组、全部 deny 自定义组和各种会话提升时，主 Agent 工具投影始终仅包含读取工具；伪造 tool call 在实际执行前拒绝。
- 主 Agent 工具表不存在 spawn/权限/导出工具。本地控制器正常创建次级 Agent；伪造 user、session、agentInstanceId、profile revision 和反馈来源均不能获取次级权限。
- 次级 deny/ask/allow 分别限制三级为 deny、deny/ask、任意三态；多权限工具取最严值，资源路径/任务类型/期限取交集。属性测试证明 `tertiary <= secondary` 始终成立。
- 会话级提升对提升前已存在及提升后新建的次级 Agent 都生效；两个次级 Agent 的个体提升互不污染。三级 Agent 不能把个体权限转给兄弟、其他次级或新会话。
- fake clock 覆盖到期边界；撤销、Agent 回收、会话关闭、profile 切换、revision/目录升级和重启重放立即失效。基础 profile 内容哈希始终不变。
- 导出 JSON/新自定义 profile 后公开权限决定可复现，但敏感字段名称和值均不存在；导出再导入不能恢复会话许可，不能作用于主 Agent。
- 关闭时导出成功、跳过、用户取消、磁盘满、权限不足、目标冲突、序列化失败和进程崩溃后，临时覆盖都不会继续有效；导出覆盖有自动备份且无锁等待。
- 多次级 Agent 导出保持彼此独立命名，不能悄悄合并为更宽权限并集。用户明确选择合并模板时也使用逐 capability 决策，不默认取 allow。
- TUI 在 80×24/120×40/60×20 下能选择具体次级 Agent、提升/撤销、查看到期和关闭导出；Headless 无参数不写文件，有参数只输出约定 JSON。

### 完成条件

- ADR-0021 的永久只读、可信次级创建、权限上限、临时提升生命周期、隔离导出和关闭清理全部有动态证据。
- 权限求交属性测试、并发/崩溃/fake clock 测试和关键分支覆盖率不低于 95%。
- `npm run check` 通过；本任务单独提交和验收。

## AR-06I：默认控制流、Agent 个体记忆隔离与三级生命周期

### 当前问题

现有实现只为次级/三级提供独立工作存档，部分设计仍把主 Agent 视为所有记忆的归并点；存档选择也可能退化为上级模型直接读取下级文件。缺少角色级/同级共享路径硬拒绝、后台报告不唤醒主 Agent、次级持续调度、三级复用/新建判定和安全关闭/接手协议，容易造成同级上下文混淆、消息列表无限增长或任务完成后后台 Agent 泄漏。

### 设计方案

1. 实现 `AgentIndividualMemoryStore` 和 `AgentMemoryNamespacePolicy`。主、次级、三级每个具体 Agent 用不可复用 `agentInstanceId` 独占 `memory-archive.json`、mission 工作存档、模型上下文、缓存、读取回执、循环预算和 mailbox 视图。目录、owner、运行时主体和 writer capability 必须一致；禁止角色级、同级组、显示名、任务 ID 或计数器共享命名空间。
2. OS/工具边界只向 Agent 运行单元挂载自己的记忆目录。直接 read/list/search/write/delete 其他个体路径全部拒绝；上级身份和相同角色都不能绕过。
3. 实现 `CrossAgentContextAttachmentController`。跨 Agent 历史只可按具体条目显式选择，经身份/可见性、revision、脱敏、哈希和 token 预算校验后生成 `externalHistoricalContext`。附件只对当前任务激活有效，不自动写入接收方长期记忆；接收方保留结论时必须写带原始来源和附件哈希的本个体观察。
4. 实现 `ConversationTaskInsertionController`。主 Agent 仅提交设计/指导插入提案；本地控制面验证目标次级、序列 revision、前驱/后继和来源。原始用户指导保持层级 0，主 Agent 派生节点只能层级 1 或以下。提交后主 Agent 立即继续对话。
5. 实现 `SecondaryContinuousDispatchLoop`。次级 Agent 在任务、反馈、权限、资源和 Git 基线变化后持续重算 ready set，并按偏序、优先级、稳定同级顺序和并发上限派发，无需主 Agent 逐任务唤醒。
6. 实现 `TertiaryAgentAssignmentPlanner` 和 `TertiarySingleChainExecutionGuard`。仅当现有个体空闲、归属次级一致、任务/mission、权限、工具、worktree、未决调用和上下文预算兼容时复用，否则新建不可复用身份。三级一次激活只执行一个任务链，只向发放源汇报，禁止链外领取、调度其他 Agent、访问兄弟记忆、写集成/目标分支或使用远端项目工具。
7. 实现 `TertiaryAgentLifecycleController`。成功、终止、无进展、消息/上下文超限、权限撤销、健康异常和会话关闭时，顺序停止派发、处理未确认调用、持久化检查点/handoff、确认终态汇报、撤销权限、注销 mailbox、处理分支/worktree 后再终止后台进程。每阶段幂等且可崩溃恢复。
8. 新接手 Agent 必须使用新身份和空白个体记忆，只接收显式选择的 handoff 附件。需要压缩旧历史时可创建独立“交接包生成”单链任务，但不得挂载旧目录、复制完整消息列表、继承权限租约或复用缓存。
9. 实现 `MainAgentReportArchiveIngestor` 和只读 `MainAgentReportReader`。次级任务终态汇报只进入独立报告索引，不唤醒主 Agent、不改变当前模型上下文、不写入主 Agent 长期记忆；后续用户轮次按任务引用、相关性和 token 预算读取。
10. GitHub、远端 Git、PR、CI、发布与产物传输工具只暴露给次级 Agent，且每次实际执行按次级当前权限、参数、用户裁决和自动备份复检；三级 Agent 永远不获得这些工具。

### 必测场景

- 两个同级 Agent 具有相同显示名、任务 ID、模型和输入时，目录、文件、上下文、缓存、读取回执、循环预算和 mailbox 仍无交集；任何角色共享路径都注册失败。
- 主、次级、三级直接访问另一个 `agentInstanceId` 记忆路径均在模型获得内容前拒绝；符号链接、联接点、硬链接、大小写、路径编码和 Unicode 碰撞不能绕过。
- 未选择条目不会出现在接收方 prompt、日志、缓存键或持久记忆。附件来源/revision/hash 可追溯，无法覆盖当前任务、用户指令或权限。
- 大量后台成功、失败和阻塞报告到达时，主 Agent 当前上下文哈希不变并能立即回答用户；后续按需读取只返回相关报告。
- 用户任务和主 Agent 派生设计插入同一偏序集时，来源和优先层正确；成环、未知锚点、陈旧 revision 和 Agent 冒充用户层级 0 被拒绝。
- 次级 Agent 可持续派发多个 ready 链，严格遵守前驱、优先级、同级稳定顺序和并发上限；复用/新建决定可解释可重放。
- 三级 Agent 只能完成一个任务链并及时向发布源报告；链外任务、兄弟消息/记忆、Agent 管理、集成分支和远端工具全部拒绝。
- 完成、终止、上下文超限、健康异常和关闭崩溃均能幂等收口，无孤儿进程、未撤销权限、重复副作用或丢失终态报告。
- 新接手个体只收到选定 handoff；旧身份、完整上下文、缓存、读取回执、mailbox 和权限租约均未继承。
- 次级执行远端项目操作时完整触发 deny/ask/allow、参数变化复检和用户拒绝；三级伪造调用 fail closed。

### 完成条件

- ADR-0022/0023 的默认控制流、个体隔离、按需报告读取、持续调度、单链执行和受控生命周期均有动态证据。
- 个体隔离属性测试、并发/崩溃/真实 IPC 测试和关键分支覆盖率不低于 95%。
- `npm run check` 通过；本任务单独提交和验收。

## AR-06J：工具说明回访、无产品数量配额与受权 Agent 通信转交

### 当前问题

当前每次 Agent 运行都可能重复注入完整工具说明，缺少“首次完整、后续按需回访”的个体回执；Agent 忘记用法和真正缺少工具也没有可区分、可逐级上报的标准协议。既有默认并发上限容易被误解为 Agent 数量配额。反馈系统还没有允许直属上级在授权后把直属下级沟通入口交给同级 Agent 的受控 grant。

### 设计方案

1. `ToolDocumentationReceiptStore` 按具体 `agentInstanceId + toolGroupIdentifier + toolGroupRevision` 记录说明内容哈希和送达状态。新个体、同级个体和接手 Agent 不继承回执。
2. `InitialToolGroupDocumentationBuilder` 首次只注入当前已分配工具的完整公开用法；`SubsequentToolGroupReminderBuilder` 在同 revision 后只注入固定 `ASTARRAY_TOOL_HELP_REQUEST_V1` 提醒。工具定义变化发送可验证 delta，无法证明完整则重发该组。
3. `ToolHelpRequestSchema` 严格区分 `usage-help` 与 `missing-capability`。模型填写请求 ID、当前任务、可选工具 ID、能力意图、阻塞原因和已知 revision；harness 注入真实 Agent、层级、直属上级、mission、来源和实际 revision。
4. `ToolDocumentationRecallController` 对当前已分配工具直接返回单工具 `ASTARRAY_TOOL_HELP_RESPONSE_V1`；对已知但未分配/权限不足返回 `known-but-not-usable` 并上报；无匹配时生成 `ASTARRAY_TOOL_CAPABILITY_ESCALATION_V1`。request ID 幂等，换词/重复请求接入活锁守卫。
5. `ToolCapabilityEscalationRouter` 固定三级默认先到所属次级，次级到会话控制面/主 Agent 报告索引。通信 grant 不改变默认路由；工具重新分配、权限提升和安装仍走原有门禁。
6. 移除 Agent 累计数、同级数和存档数的硬编码产品配额。单会话保留一个当前主 Agent；并发资源通过队列、暂停、Provider 限流和受控回收处理，不得因历史实例数量拒绝新建。
7. 在权限目录增加 `agent.communication-delegate`。`AgentCommunicationDelegationController` 只接受直属上级为直属低一级 target 向一个具体同级 recipient 发起的请求；Assist 逐次询问，Devolve/自定义按三态执行。
8. `DelegatedAgentCommunicationGrantStore` 保存 grantor/recipient/target、mission/任务、消息类型、instruction 标志、回复/抄送路由、到期、最大在途量、revision 和裁决引用。模型只见不透明 handle ID，不见 IPC 地址、socket、mailbox 路径、进程句柄或能力令牌。
9. 直连消息仍通过独立反馈进程、使用真实发送者来源并保留转发来源。grant 默认不转移下级归属、任务、偏序集、Git、记忆、工具分配、权限或默认上报链，且不可再次转授权。
10. 每次投递前复检 Agent 存活/关系、任务/mission、profile revision、到期、撤销、消息类型和在途量。任一条件变化立即失效，重启恢复不能复活旧 grant。

### 必测场景

- 首次完整说明、同 revision 100 次后续提醒、新 Agent、接手 Agent、工具新增/删除/schema 变化和回执损坏恢复。
- usage-help/missing-capability 的成功、未知工具、未分配、权限不足、陈旧 revision、重复 ID、伪造身份、非法字段和换词循环。
- 工具帮助不泄露未分配 schema、内部规则或凭据，不授予工具/权限，不绕过安装门禁。
- 三级帮助/缺失能力默认只到所属次级；兄弟、被转交通信的同级和消息正文无法改写上级路由。
- 压力创建、排队、归档和回收大量各层个体，不出现累计/同级硬上限错误；资源不足只形成可解释排队或受控回收。
- 合法“次级 A 把直属三级 X 的沟通句柄授权给次级 B”可双向通信并保留来源；主/三级或非直属/非同级/跨 mission 请求拒绝。
- Assist 允许/拒绝/超时、Devolve allow、自定义 deny、到期、撤销、profile revision 变化、Agent 回收和重启恢复均正确。
- recipient 无法用 grant 读取 target 记忆、派发任务、修改工具/权限、接管 Git、转授权或联系未授权 Agent。

### 完成条件

- ADR-0024 的首次/后续说明、标准格式、逐级上报、数量语义和通信 grant 均有动态证据。
- 协议、身份、幂等、授权、并发、真实 IPC、失效与崩溃恢复关键分支覆盖率不低于 95%。
- `npm run check` 通过；本任务单独提交和验收。

## AR-07：覆盖率、npm tarball 和最终文档验收

### 当前问题

当前总体覆盖率通过，但反馈进程等关键模块的分支覆盖没有达到实施计划所期望的 95% 关键路径门槛。`PLAN_STATUS.md` 也需要与动态验收结果重新对齐。

### 实施步骤

1. 为以下关键目录设置并验证独立覆盖率门槛，分支覆盖率目标不低于 95%：
   - 独立反馈进程及 IPC schema。
   - 权限和 Agent 执行闸门。
   - 受保护存储策略。
   - 备份创建、读取、恢复和删除控制器。
    - Agent 身份分配与工作存档选择。
    - Ponder 本地只读策略与敏感操作分类。
    - 完成事件解析、本地验收、早停看门狗和续跑协调器。
    - 全模式敏感内容禁读、反自指读取账本、循环守卫、严谨性策略和证据门禁。
2. 补充以下类型测试：
   - 属性测试：路径编码往返、无碰撞、路径规范化、资源规范化、读取范围包含关系和查询指纹等价类。
   - 并发测试：保管库修订、审计链、反馈队列、读取 single-flight、循环图和证据来源去重。
   - 故障注入：进程退出、部分写入、损坏 journal、过期能力、DLP 扫描失败、搜索代理断网、单调时钟跃迁和循环账本损坏。
   - 安全反例：身份冒充、权限绕过、符号链接 TOCTOU、重放授权、秘密旁路、自指提示注入、证据伪造和换词活锁。
   - TUI 交互：协同模式警告/暂停/允许/拒绝/超时，以及证据冲突、不足和“等待用户判断”展示。
3. 运行完整门禁：

   ```powershell
   npm run typecheck
   npm run lint
   npm run test
   npm run test:coverage
   npm run build
   npm run check
   npm pack --dry-run
   npm pack
   ```

4. 在全新目录从 tarball 安装，不得从仓库源码或 workspace 依赖偷跑。
5. tarball 验证以下入口：
   - Linux/macOS shell shim。
   - Windows `.cmd` shim。
   - `astarray --version`、`--help`、`doctor`、`run --mock`。
   - 独立 `astarray-feedback-process` 入口。
   - 非交互终端和交互 TUI 的退出码。
6. 运行 `npm audit` 并记录生产依赖风险。网络不可用时明确记录为未验证，不得写成通过。
7. 至少在 Node 20 和当前支持的较新 Node 版本验证；CI 覆盖 Windows、Linux，并在可用时覆盖 macOS。
8. 更新：
   - `PLAN_STATUS.md`
   - `IMPLEMENTATION_PLAN.md` 中实际发生变化的验收说明
   - 必要的 ADR、威胁模型和 IPC 协议文档
   - README 中 npm 安装、模式名称和风险行为说明
9. 只有全部动态测试通过后，才可将本任务书状态改为“完成”。

### 最终安全验收清单

- [ ] 普通工具无法发现或访问备份物理存储。
- [ ] 协同模式删除备份会真实暂停且必须逐次授权。
- [ ] 放权模式删除备份无提示但有高优先级、完整哈希链日志。
- [ ] 思索模式禁止删除备份。
- [ ] 思索模式可读取普通项目内容并经隔离代理搜索资料，但写入、进程、通用网络、凭据和备份访问均被本地硬拒绝。
- [ ] 敏感操作分类断网可用且不依赖云端/AI 放行。
- [ ] `.env`/`.env.*`、私钥和凭据内容在三种模式及全部旁路中均不可进入模型，无授权例外。
- [ ] 自指文件、路径别名、范围切片和等价内容抖动不能造成重复正文读取。
- [ ] 工具/资源环、Agent 回派乒乓、搜索换词和无进展续跑均在本地有界暂停。
- [ ] 高严谨性任务强制形成可追溯证据包；证据按搜索/实验/推理分层并保留冲突和局限。
- [ ] 事实工具不自动给出最终合格/安全结论，用户判断边界在 TUI/CLI 中明确可见。
- [ ] 协同模式在任意代码库、依赖、运行时、插件、工具链或系统包安装前，都会先询问用户是否已有可用资源；已有资源只做只读验证，不兼容时不会自动安装。
- [ ] Assist 安装独立开关默认关闭；开启不等于授权，每个精确安装计划仍须用户 `allow-once`，普通会话授权、Agent 声明和非交互 `--yes` 均不能放行。
- [ ] 安装授权的来源、版本/commit、目标、作用域、参数、安装脚本、模式或开关 revision 变化后失效；等待用户期间不持锁。
- [ ] Devolve 的全部公开权限默认直接允许且每项可独立切换禁止/询问/直接允许；Assist 使用互不影响的独立默认矩阵，Ponder 不出现权限编辑入口。
- [ ] 用户可创建、命名、复制、重命名和使用不设产品数量上限的自定义权限模式；运行记录绑定不可变 ID 和 revision，而不是只依赖名称。
- [ ] 新工具和多权限工具按版本化权限目录及最严格决定执行；未知/未分类权限不扩大访问，配置变化使旧授权和缓存失效。
- [ ] 设置、帮助、导出、普通日志/错误/审计和模型描述只呈现公开可配置权限，不显示、暗示或列举内部执行层。
- [ ] 主 Agent 在全部内置、自定义模式及会话临时提升下始终只能调用读取类工具；次级 Agent 由可信本地控制器创建并持有执行权限。
- [ ] 权限组和临时提升只作用于具体次级 Agent；三级 Agent 的三态权限、工具与资源范围始终不宽于所属次级 Agent。
- [ ] 会话临时提升按具体次级 Agent 隔离，关闭、到期、撤销、回收或 profile revision 变化后失效且不修改基础 profile。
- [ ] 关闭会话可导出当前公开有效权限，但导出不包含会话/Agent 身份、nonce、签名、令牌、一次性许可、到期计时器或内部字段，重新导入不恢复授权能力。
- [ ] 反馈来源由认证通道注入，不由模型字段决定。
- [ ] 每个 Agent 来源具体到不可复用的 `agentInstanceId`。
- [ ] 任务图、反馈和存档使用同一 Agent 实例身份。
- [ ] 次级、三级 Agent 各自拥有独立工作存档。
- [ ] 主、次级、三级以具体且不可复用的 `agentInstanceId` 为唯一记忆所有者；同级之间也不共享存档、上下文、缓存、读取回执或消息视图。
- [ ] 任意 Agent 不能直接读取另一个 Agent 的记忆路径；跨 Agent 只传带来源/revision/hash 的当前任务附件，且不自动写入接收方长期记忆。
- [ ] 上级 Agent 只附加明确选择的存档条目。
- [ ] 次级 Agent 负责 Git 分流、审查和合并，三级 Agent 的提交均隔离且可追溯。
- [ ] Agent 待办任务以独立偏序集存储，用户任务默认最高层，自动任务无法提权。
- [ ] 次级 Agent 可将整条链打包给三级 Agent，发布者可随时查询一致状态快照。
- [ ] 主 Agent 提交任务插入提案后持续响应用户；后台报告只入索引，后续对话按需只读加载，不抢占或污染当前上下文。
- [ ] 次级 Agent 持续调度并决定三级 Agent 复用、新建和关闭；三级一次激活只执行一个任务链并向发放源报告。
- [ ] 三级 Agent 上下文过长或终止后可由新个体通过显式 handoff 接手，不复用身份、完整历史、缓存或权限；关闭流程无孤儿进程。
- [ ] GitHub、远端 Git、PR、CI、发布和产物传输由次级 Agent 在有效权限内控制，三级 Agent 无相关工具。
- [ ] 次级/三级每个具体 Agent 首次接收工具组完整公开用法，同 revision 后续只收标准提醒；新个体和工具 revision 变化不会错误复用回执。
- [ ] `ASTARRAY_TOOL_HELP_REQUEST_V1` 能区分忘记用法和缺少能力；已分配工具直接按单工具回复，未分配/不存在能力逐级上报且不扩大工具或权限。
- [ ] 三级 Agent 的工具帮助默认上级始终是所属次级；通信 grant、兄弟 Agent 和消息正文不能篡改路由。
- [ ] 各层 Agent 无累计/同级产品数量配额；资源限制只导致排队、暂停或受控回收。
- [ ] 直属上级可在授权后把直属低一级 Agent 的限定沟通句柄交给具体同级 Agent；grant 不暴露底层凭据、不转授权、不转移任务/记忆/工具/Git/权限并可及时失效。
- [ ] 破坏性 Git 操作会在底层工具中自动创建受保护恢复点。
- [ ] 所有破坏性变更在执行工具中自动备份。
- [ ] 保管库并发操作不丢修订，审计链不分叉。
- [ ] 文件、目录、空目录和原始缺失状态可精确恢复。
- [ ] 模型完成事件只有在本地验收通过后才结案；伪造、重放和提前声明均无效。
- [ ] 早停从检查点有界续跑，不并发重复请求、不重复不确定副作用，三次后明确失败反馈。
- [ ] 关键安全模块分支覆盖率达到 95%。
- [ ] `npm run check` 通过。
- [ ] tarball 隔离安装和全部 CLI 入口通过。
- [ ] 文档状态与动态验收证据一致。

## 6. 建议的测试分层

### 6.1 单元测试

- schema、状态机、身份生命周期、授权一次性消费、安装请求参数哈希与开关 revision 绑定。
- 路径规范化、受保护路径判定、编码和解码。
- 备份 manifest 版本迁移、哈希校验、快照类型。
- 存档选择和来源链校验。
- Ponder 只读工具矩阵、本地敏感分类、敏感资源身份/DLP、读取回执、循环图、严谨性策略、证据 schema、完成事件 schema、防重放和早停分类。

### 6.2 组件测试

- 普通文件工具加 `ProtectedStoragePolicy`。
- 删除备份控制器加 Agent 执行闸门。
- 独立反馈进程加真实 IPC。
- `MissionOrchestrator`、`DagScheduler`、反馈注册和存档 writer 的身份一致性。
- `AgentIndividualMemoryStore`、`AgentMemoryNamespacePolicy` 和 `CrossAgentContextAttachmentController` 的跨个体隔离及同级防混淆。
- `ToolDocumentationReceiptStore`、`ToolDocumentationRecallController`、`ToolCapabilityEscalationRouter` 和 `AgentCommunicationDelegationController` 的协议、身份与授权组合。
- Ponder policy wrapper 加 OS 只读边界；CompletionVerifier、Watchdog 与 ContinuationCoordinator 的状态一致性。
- 敏感内容策略覆盖各适配器；ReadSuppressionLedger 加 CycleGuard；事实搜索代理加 EvidenceCompletionGate。
- InstallationOperationClassifier、ExistingResourceInquiryController、AssistInstallationSettings 与逐次授权控制器的本地组合门禁。
- PermissionCapabilityCatalog、PermissionProfileStore、自定义 profile 控制器、最严格多权限裁决和 profile revision 失效链路。
- MainAgentReadonlyToolProjection、SecondaryAgentSessionController、SessionPermissionElevationController、TertiaryPermissionDelegationGuard 与权限配置导出器。

### 6.3 集成测试

- 从任务派发到 Agent 反馈、存档、重新派发的完整链路。
- Assist 模式删除备份的暂停、用户授权、执行、审计和恢复。
- Devolve 模式无提示删除与高优先级日志。
- 目录备份后发生新增、删除、类型变化，再精确恢复。
- Ponder 读取/越权反例全链路；模型早停、检查点、续跑、最终完成/阻塞的完整链路。
- 三模式敏感禁读全链路；自指读取/Agent 乒乓；高严谨性搜索、实验、证据不足和用户判断展示。
- Assist 从资源需求、已有资源询问、只读兼容性验证到精确安装授权/拒绝的完整链路；并发安装请求隔离且等待不持锁。
- Devolve/Assist 独立编辑、Ponder 无编辑入口、自定义 profile 全生命周期、模式选择和任务/缓存/审计 ID 绑定完整链路。
- 主 Agent 只读投影、可信次级创建、会话临时提升、三级权限求交和关闭导出/清理完整链路。
- 主 Agent 插入提案与报告只入档、次级持续派发、三级复用/新建/单链执行/受控关闭和显式 handoff 的完整链路。
- 首次工具组说明、后续标准提醒、三级逐级工具回访、缺失能力上报，以及同级经授权直连直属下级的完整链路。

### 6.4 端到端与打包测试

- 从 npm tarball 安装后启动 TUI。
- 独立反馈进程异常退出和自动恢复。
- Windows 路径、Unicode 文件名和 shell shim。
- 无 TTY、只读工作区、权限不足和损坏状态目录。
- tarball 环境中安装开关默认关闭；无交互确认时任何依赖、代码库或工具链安装请求 fail-closed。
- tarball 环境中创建大量命名权限组、重启后选择保持、导入导出和配置损坏恢复；用户可见产物只包含公开权限目录。
- tarball 环境中关闭会话导出公开有效权限；重启导入后无临时授权延续，主 Agent 仍为只读。
- tarball 环境中并行启动同级 Agent，验证按个体隔离的存档/缓存/mailbox；后台报告不阻塞 TUI 输入，三级关闭/换新无孤儿进程。
- tarball 环境中验证工具说明回执跨重启、无 Agent 数量配额的排队压力、通信 grant 授权/撤销/到期和真实反馈子进程投递。

### 6.5 非功能测试

- 并发压力：至少 50 个保管库事务和 1000 条反馈消息。
- 恢复性能：大型目录快照期间保持有界内存。
- 等待策略：正常轮询不超过 60 秒，`pn` 不超过 3 小时。
- 日志脱敏：能力令牌、恢复凭证、对象哈希和物理保管库路径不进入模型可见日志。

## 7. 风险与注意事项

1. **不要先改 UI 再补核心约束。** 权限、身份、暂停和备份必须在核心执行边界成立，TUI/GUI 只能展示和调用。
2. **不要持锁等待用户。** 用户授权可能持续很久；持有文件锁会造成保管库和 Agent 调度死锁。
3. **不要把 schema 校验当作认证。** 格式正确的 `agentInstanceId` 仍可能是伪造值，必须绑定可信发送能力。
4. **不要用路径字符串前缀做安全判断。** Windows 大小写、链接、联接点和不存在目标都会产生绕过。
5. **不要把恢复实现成简单覆盖。** 精确恢复必须处理额外文件、空目录、类型切换和原始缺失状态。
6. **不要用固定临时目录。** 使用排他创建的唯一目录，并验证其规范化绝对路径位于预期根目录。
7. **不要弱化覆盖率门槛。** 新增分支必须有允许、拒绝、超时、损坏和并发测试。
8. **不要默认注入 Agent 历史。** 存档附加是显式、逐条、带修订号的选择行为。
9. **不要让三级 Agent 自行集成。** 三级 Agent 只提交自己的隔离分支；次级 Agent 统一审查、处理冲突和合并。
10. **不要把偏序集退化为普通 FIFO。** 发布者未指定位置时保持不可比，调度只在 ready set 内比较优先级。
11. **不要让自动任务冒充用户任务。** 层级 0 必须由认证用户来源获得，Agent/system/工具只能进入层级 1 或以下。
12. **不要把 Git 当作备份库。** reset、clean、rebase 和引用删除仍须工具内自动恢复点。
13. **不要在输出中暴露备份内部信息。** 模型只获得受控业务内容和公开元数据。
14. **不要用内存计数器充当永久身份。** 进程重启会复用；使用随机不可复用实例 ID。
15. **不要一次修改过多。** 每轮一个主任务；高风险变更完成目标测试和全量门禁后再开始下一项。
16. **不要自动删除旧格式数据。** 迁移必须先备份、验证内容身份，遇到碰撞立即停止。
17. **不要把“只读”交给模型判断。** 工具 ID、参数、真实路径、打开方式和 OS 能力必须由本地验证，未知操作默认拒绝。
18. **不要把输出结束当作完成。** 必须同时具备明确完成控制事件与本地验收证据；工具输出里的标识不能被解析成控制事件。
19. **不要无限续跑或重复副作用。** 先检查点、后续跑，限制尝试次数；无法确认的非幂等写入必须暂停裁决。
20. **不要为 Devolve 或“验证需要”开放秘密。** `.env`、私钥和凭据全模式禁读且没有会话授权例外。
21. **不要只按路径字符串做读取时间锁。** 必须绑定具体 Agent/任务和规范资源身份，并识别链接、硬链接、重叠范围和参数噪声。
22. **不要让数据变成指令。** 文件、网页、工具输出和历史存档中的自指文字默认不受信，每个后续调用重新鉴权。
23. **不要把事实工具变成裁判。** 它只能提供证据关系、冲突和局限，最终是否合格由用户决定。
24. **不要把安装开关当成授权。** Assist 必须先问用户是否已有资源；即使开关开启，每个来源、版本和目标明确的安装仍需一次性确认。
25. **不要用安装失败代替资源询问。** 探测命令可能已经下载、运行脚本或改写 lockfile；先询问，再进行只读兼容性检查。
26. **不要把自定义模式实现成字符串枚举扩展。** 使用不可变 profile ID、revision 和目录版本；名称只用于显示。
27. **不要设置自定义模式数量配额。** 使用分页、虚拟列表和流式存储解决规模问题，不能用 `maximumProfiles` 一类产品限制简化实现。
28. **不要在产品面呈现内部执行层。** 不生成禁用按钮、锁图标、提示项、帮助说明或导出字段；普通拒绝结果也不暴露规则类别。
29. **不要把 profile 套给主 Agent。** profile 和临时提升只计算次级 Agent 权限；主 Agent 模型工具投影永久只读。
30. **不要让次级 Agent 向下扩权。** 三级权限必须逐项与次级有效权限求交，不能把 ask 当 allow，也不能扩大资源范围或有效期。
31. **不要把导出配置做成授权快照。** 只导出公开决定和范围，彻底剥离会话、Agent、nonce、签名、令牌、许可和计时器。
32. **不要按角色或同级组共享记忆。** 唯一所有权键是完整不可复用的 `agentInstanceId`；显示名、角色、任务 ID 和计数器都不能作为共享存档键。
33. **不要让上级模型直接读取下级记忆文件。** 只能由本地附件控制器按条目生成当前任务有效、带来源与哈希的不可变附件。
34. **不要让后台汇报唤醒主 Agent。** 先入独立报告索引，只有后续用户对话需要时才只读选择。
35. **不要直接 kill 三级 Agent 代替收口。** 先处理未确认调用、handoff、反馈、权限、mailbox 和 Git 资源，再关闭进程。
36. **不要每轮重复注入完整工具组。** 首次或 revision 变化才发送完整/差异说明，后续只发固定帮助格式。
37. **不要把工具帮助当成授权。** 未分配、权限不足或需要安装时必须上报并经过原门禁。
38. **不要暴露下级通信底层信息。** 只传不透明、可撤销、范围受限的 communication handle。
39. **不要把并发上限做成 Agent 数量配额。** 资源不足时排队、暂停或回收，不能按累计实例数拒绝。

## 8. OpenCode 可直接使用的任务提示模板

每轮把 `<TASK_ID>` 替换成唯一主任务编号：

```text
完整读取 AGENTS.md、agent-main-architecture.md、designtodo.txt、
IMPLEMENTATION_PLAN.md、PLAN_STATUS.md 和 AUDIT_REMEDIATION_TASKS.md。

本轮只执行 <TASK_ID>，不得顺带执行下一个 AR 任务。先检查 git 状态和相关实现，
先增加能够复现问题的失败测试，再编码修复。生产代码使用含义完整、可读性好的变量名，
布尔变量使用 is/has/can/should 前缀，时间量带单位。

任何删除、删减、替换、截断或覆盖必须由工具在变更前自动备份；不要在模型侧创建备份。
不要持锁等待用户授权。反馈工具必须保持独立进程，Agent 来源必须认证到具体且不可复用的
agentInstanceId。默认不附加任何次级或三级 Agent 存档，只使用任务中显式选择的条目。

Ponder 仅能使用本地只读白名单，敏感操作分类不得请求云端或交给 AI 裁决。模型只有返回
ASTARRAY_TASK_COMPLETION_V1 且本地验收通过才算完成；疑似早停从检查点最多续跑 3 次，
不得重复未确认的非幂等副作用。

本轮若涉及新安全任务，只能执行 AR-06C、AR-06D、AR-06E、AR-06B、AR-06F、AR-06G、AR-06H、AR-06I 或 AR-06J 中的一个。所有模式禁止
读取 .env、私钥和凭据；高严谨性任务必须形成辅助证据包；重复读取和循环调用由本地守卫暂停。

若本轮执行 AR-06F，任何代码库、依赖、运行时、插件、工具链或系统包安装前必须先询问用户
是否已有可用资源；开关默认关闭且只允许提出申请，每次安装仍使用绑定精确计划的 allow-once。

若本轮执行 AR-06G，Devolve 的公开权限默认全部 allow，Assist 使用独立矩阵，Ponder 不可编辑；
自定义权限组不限数量并使用 ID/revision。用户产品面只能看到公开权限目录，不呈现内部执行层。

若本轮执行 AR-06H，主 Agent 永久只读；profile/临时提升只授予当前会话的次级 Agent（可为会话级或具体个体），并限制三级权限上限。
会话关闭导出只含公开权限配置，不得携带任何授权能力，随后无条件撤销会话临时提升。

若本轮执行 AR-06I，每个主、次级、三级 Agent 必须以完整不可复用 agentInstanceId 独占记忆、上下文、缓存和消息视图；同级也不共享。
跨 Agent 只经本地控制器传递当前任务有效的显式附件。主 Agent 汇报只入索引；次级持续调度并收口三级生命周期；三级一次只执行一条任务链。

若本轮执行 AR-06J，次级/三级 Agent 首次接收完整工具组说明，后续只收 ASTARRAY_TOOL_HELP_REQUEST_V1 提醒；帮助不构成授权，三级默认上报所属次级。
Agent 无累计/同级产品数量配额。下级通信只能通过不可转授权的不透明 grant 交给具体同级，不得转移任务、记忆、工具、Git 或权限。

完成后运行目标测试、npm run check，并报告修改文件、测试数字、剩余风险和是否满足本任务
全部完成条件。若预计超过 3 小时，在可构建边界拆分；pn 等待上限为 3 小时。
```

## 9. 完成定义

本任务书只有在 AR-00 至 AR-07（包括 AR-06A 至 AR-06J）全部满足各自完成条件、最终安全清单全部勾选、tarball 隔离安装通过且文档与动态证据一致时，才能标记完成。任何仅有代码审阅、静态推断或“已有测试看起来通过”的结论都不能替代动态验收。
