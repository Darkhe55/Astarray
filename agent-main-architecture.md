# 主 Agent 架构设计草稿

> 草稿状态：v0.2（待评审）
> 关联约束：见 `designtodo.txt`（缓存有效性、token 消耗、并发数量、主-子 agent 解耦通信）

---

## 1. 核心思想

单一个**主 Agent** 承担用户会话与自己的个体记忆，通过**三种运行模式**按信任梯度切换；每个次级、三级 Agent 也拥有独立个体记忆，彼此不共享上下文：

```
信任梯度（由低到高）：

  Ponder（思索） ─────→ Assist（协同） ─────→ Devolve（放权）
  本地只读，零副作用  次级受限执行+询问      次级默认允许+自由调度
  无子 agent          次级+三级 agent 分层    多级子 agent 自由委派
```

- **模式是会话为次级 Agent 选择的权限策略状态**，不是三个独立 agent；它不扩大主 Agent 的工具范围。
- **主 agent 不陷入任务流程**：任务派发后立即回到待机，持续接收用户新消息（本版核心原则，见 §3.1）。
- 每个 Agent 个体的记忆始终归属自身 `agentInstanceId`；不存在角色级、同级或主子共享上下文。跨 Agent 仅传递带来源的反馈或显式选择的当前任务附件。
- 主 Agent 在所有内置、自定义模式及会话临时提升期间都只有读取类模型工具。任务派发由本地会话控制器创建具体次级 Agent，不通过主 Agent 的 spawn/执行工具完成。
- 任何时刻必须可以安全降级（Devolve → Assist → Ponder）。

---

## 2. 三种内置模式与自定义权限模式

### 2.1 Ponder 模式（思索模式）

| 项 | 定义 |
|---|---|
| 工具 | **本地只读白名单**：项目文件读取/列举/检索、只读任务状态、固定 Git 只读查询 |
| 行为 | 基于会话与项目现状进行推理、解释、方案讨论、复盘 |
| 记忆 | 全部保存在主 agent 上下文/长期记忆中 |
| 权限 | 无写入、进程执行、网络或外部副作用 |
| 适用 | 头脑风暴、概念讲解、决策讨论、需求澄清、结果审阅 |

**设计要点**：
- 该模式下主 agent 只读会话与允许的项目内容，不做任何写操作；不产生任务、记忆、缓存或遥测文件。
- 白名单由本地 harness 暴露并在执行前复检；未知、可能写入或副作用无法证明的工具直接本地拒绝。通用 shell、网络、外部连接器、凭据、`backupVault` 和 `deleteBackup` 均不可用。
- 敏感操作检测使用本地版本化工具元数据、确定性参数/真实路径检查和操作系统只读边界，不依赖云端或 AI 的判断。
- `.env`、`.env.*`、私钥、凭据库和本地 DLP 命中的内容在三个模式下都禁止进入模型；该规则先于模式授权执行，Devolve 也无例外。
- 长会话依赖主 agent 的记忆压缩/摘要机制（见 §7）。
- 是默认兜底模式：任何模式下出现不确定性，可主动降级到此模式与用户对齐。

### 2.2 Assist 模式（协同模式）

| 项 | 定义 |
|---|---|
| 工具 | 白名单内的有限工具；**用法说明不注入主 agent，只注入执行层** |
| 行为 | 受限执行任务；涉及权限敏感操作**必须先询问用户** |
| 询问 | 询问时**同时提供：① 拟调用的内容（参数摘要）② 简要说明** |
| 结构 | 三级分层：主 agent（接收/转交）→ 次级 agent（调度）→ 三级 agent（执行） |
| 记忆 | 主、次级、三级各自持有个体记忆域；主 Agent 另有只读报告索引，跨个体不合并上下文 |
| 适用 | 日常任务执行：检索、生成文件内容草稿、低风险编辑 |

**权限门禁规则**（本模式核心）：

```
执行层想调用工具 T
  │
  ├─ T ∈ 白名单只读工具 → 直接调用，无需询问
  ├─ T ∈ 白名单受限工具 → 必须询问用户 ↓
  └─ T ∉ 白名单          → 拒绝调用；可显示由用户打开权限组选择器的选项
```

**询问格式（固定模板，保证可机器解析/可展示）**：

```
[权限请求 #id]
调用: <工具名>(<参数摘要，敏感值打码>)
说明: <一句话：为什么调用、会产生什么影响、失败成本>
操作: 允许 / 拒绝 / 修改参数
```

- 询问为**阻塞式**：未收到用户答复前，不得继续推进该调用链。
- 用户可"修改参数"，用修改后的参数重试。
- 同一普通权限请求可配置"本次会话内记住"（仅提升到会话级白名单，不跨会话）；安装授权和删除备份授权不得记住。

**白名单示例（示意）**：

| 级别 | 工具 | 是否询问 |
|---|---|---|
| 只读 | 文件读取、搜索、网络只读查询 | 否 |
| 受限 | 写临时文件、本地小范围修改 | 是 |
| 专用安装门禁 | 代码库、依赖、运行时、插件、工具链、系统包 | 先确认已有资源；开关开启后仍逐次询问 |
| 禁止 | 删除、覆盖、资金、外部发布 | 一律拒绝，转 Devolve |

#### 2.2.1 Assist 安装前置确认与独立开关

- “安装”按实际效果识别，不依赖命令名称。范围包括克隆/下载并落地可复用代码库，新增或更新 npm/pnpm/yarn、Python、Rust 等项目依赖，安装运行时、编译器、浏览器、插件、技能、模型、工具链和系统包，以及会运行生命周期安装脚本、改写 lockfile/vendor 目录或改变依赖解析结果的操作。
- 在任务第一次需要某项资源、但在任何安装探测或安装命令之前，系统必须暂停对应 Agent，向用户列明用途、所需能力和已知候选项，询问“是否已经有可用的代码库、依赖、运行时或工具链”。不得仅凭项目文件、Agent 推断或命令失败假定用户没有。
- 用户回答已有时，系统只请求用户选择或提供资源位置，然后通过只读、无安装副作用的兼容性检查验证版本、完整性、来源和任务适用性；验证失败后必须把差异反馈给用户，不能自动转入安装。
- 用户明确回答没有可用资源后，才进入安装能力判断。设置提供独立布尔开关 `isAssistInstallationEnabled`，默认 `false`。关闭时 Assist 一律拒绝安装并说明如何开启；开启只表示允许提出安装申请，不构成任何一次安装授权。
- 每次安装必须生成独立的 `assist-installation-request`，绑定具体 `agentInstanceId`、任务、包/仓库来源、精确版本或 commit、目标路径/作用域、包管理器、命令参数、预计文件变化、安装脚本、网络访问和风险摘要。用户只能选择允许本次、拒绝或修改参数。
- 安装授权必须 `allow-once`，不得使用 Assist 的十分钟会话授权、批量许可、相似命令复用或上级 Agent 代答。版本、来源、commit、目标、作用域、参数、lockfile 或安装脚本摘要任一变化都使授权失效并重新从逐次确认开始。
- 等待“是否已有资源”或安装授权时不得持有文件锁、任务领取锁或包管理器锁；对应 Agent 进入明确的 `awaiting-existing-resource-answer` 或 `awaiting-installation-authorization` 状态，其他无依赖任务可继续。
- 所有判断和门禁均由本地设置存储、确定性操作分类器和执行 wrapper 完成。模型不能声明开关已开启、用户已回答或安装已授权；实际执行前还要复检模式、开关 revision、授权 nonce、参数哈希、目标边界和敏感内容策略。
- 设置开关变更本身属于受保护配置写入：只能由认证用户经 TUI/CLI 设置界面操作，变更前由工具自动备份并写审计；Agent、项目文件、插件和模型输出无权修改。

### 2.3 Devolve 模式（放权模式）

| 项 | 定义 |
|---|---|
| 控制权 | 次级 Agent 的公开权限组默认全部 `allow`；主 Agent 仍保持只读 |
| 子 agent | 本地会话控制器创建次级 Agent；次级 Agent 可在权限上限内创建、分派和回收三级 Agent |
| 记忆 | 每个具体 Agent 独占自己的记忆、上下文、缓存与消息视图；终态只传结构化报告/附件 |
| 与 Assist 的区别 | 两者均由次级 Agent 调度；Devolve 的次级权限默认全部允许，Assist 使用独立保守矩阵 |

Devolve 的“无应用层门禁”由默认权限配置表达，而不是写死分支。设置页必须列出可配置权限目录中的每一项，并允许认证用户独立选择 `deny`（禁止）、`ask`（询问）或 `allow`（直接允许）；Devolve 出厂配置的全部可配置项均为 `allow`。底层执行约束不属于该目录。

### 2.4 可配置权限组与自定义模式

- Assist 与 Devolve 是两个独立、可持久化的内置权限组；修改一个不得影响另一个。Ponder 的权限组由程序签名版本冻结，设置界面只能查看，不能修改、复制回写或用导入覆盖。
- 权限项使用三态决定：`deny` 不暴露且执行层拒绝；`ask` 暂停具体 Agent 并向认证用户申请本次授权；`allow` 无应用层询问直接进入执行前本地复检。设置页的每一项只显示可配置权限 ID、中文名称、作用范围、当前值和默认值，不显示或暗示未进入目录的底层约束。
- Devolve 的内置默认值全部为 `allow`。Assist 使用单独默认矩阵：普通本地读取和只读任务状态为 `allow`；普通项目创建/修改、受控进程、只读网络、常规 Git 写入和备份删除为 `ask`；删除/覆盖、历史改写、远端发布、系统范围修改、资金和其他高风险能力默认为 `deny`。安装项默认为 `ask`，但仍串联 §2.2.1 的已有资源询问、独立安装开关和精确 `allow-once`，三态 `allow` 不能跳过 Assist 的强制安装确认。
- 用户可创建任意数量的自定义模式；系统不得设置产品级数量上限，仅受实际持久化资源和通用输入大小边界约束。每个模式使用不可复用 `permissionProfileId` 和用户设置的可辨识 `displayName`；名称按 Unicode 规范化后在活动权限组中不区分大小写唯一，可重命名但 ID 不变。
- 创建时可选择“空白（全部 deny）”或复制 Assist、Devolve、Ponder 的可配置视图或任一自定义模式。复制得到独立 revision，后续互不联动。自定义模式可逐项配置全部权限目录，也可设置新增权限项的 fallback 决策；fallback 默认 `deny`。
- 自定义模式不是新的硬编码 `AgentMode` 枚举值。运行态使用 `permissionProfileReference = builtin:<ponder|assist|devolve> | custom:<permissionProfileId>`；反馈、任务、缓存、审计和授权均记录不可变 ID、displayName 快照和 profile revision，不能只记录可重名/可变名称。
- 权限目录是本地、版本化和可审计的 `PermissionCapabilityCatalog`。任何 Agent 工具注册前必须映射一个或多个权限 ID；未分类工具不得执行。用户直接创建、重命名、复制、导入/导出和删除权限组属于认证设置控制面，不作为 Agent 可调用权限项。新增权限时，Devolve 的未修改出厂策略按其默认 `allow`，Assist 按目录给出的 Assist 默认值，自定义模式按各自 fallback 处理，并在设置界面标记“新增未审阅”。
- 权限组支持创建、重命名、复制、导出、导入、重置和删除自定义项。任何覆盖、重置、导入或删除先由底层设置工具自动备份；删除当前正在使用的模式前必须先切换到有效模式。不得删除三个内置模式。
- 模式切换和权限修改只能由认证用户或用户预先授权的控制面完成；Agent、模型、项目文件、插件和工具输出不能创建、选择或修改权限组。权限设置变更后 revision 增加，旧授权和在途但尚未执行的调用必须重新鉴权。

### 2.5 权限作用域、会话临时提升与导出

- 权限 profile 不作用于主 Agent。主 Agent 的模型可见工具始终是本地读取类白名单：普通项目读取/列举/检索、只读任务状态、固定 Git 只读视图和其他被归类为读取且适合主 Agent 的专用查询。任何写入、进程、安装、网络写入、Agent 管理或外部副作用工具均不向主 Agent暴露，也不能通过 Devolve、自定义 profile 或临时提升加入。
- 主 Agent 接收用户任务后，向本地 `SecondaryAgentSessionController` 提交任务描述。控制器创建具体且不可复用的次级 `agentInstanceId`，把当前 profile 的公开决定快照作为该次级 Agent 的权限上限。该控制动作不是模型工具调用，主 Agent不能填写或伪造授权字段。
- 次级 Agent 是唯一执行权限持有者和三级 Agent 权限发布者。三级 Agent 的每项决定不得宽于次级 Agent 当前有效决定，严格顺序为 `deny < ask < allow`：次级为 deny 时只能分发 deny；为 ask 时只能分发 deny/ask；为 allow 时可分发任一三态。多项权限、资源范围、目标路径、工具子集和有效期同时取交集。
- 三级 Agent 不直接获得 profile，也不能请求修改 profile。其需要更高权限时向所属次级 Agent 反馈；次级 Agent 再通过本地控制面向用户提出临时提升请求。上级不能把一次提升转授给兄弟次级 Agent或其他会话。
- `SessionPermissionElevationController` 允许认证用户为当前 `sessionId` 创建会话级临时覆盖，默认作用于该会话全部现有及后续次级 Agent；用户也可显式创建只绑定具体次级 `agentInstanceId` 的个体覆盖。每条覆盖绑定作用域、capability ID、原决定、新决定、资源范围、创建时间、可选到期时间、基础 profile ID/revision、会话权限 revision 和一次性用户裁决记录；只能提升公开权限，也允许随时撤销或降回基础值。
- 临时提升不修改、覆盖或自动保存基础 Assist/Devolve/自定义 profile，不跨会话、不进入普通十分钟工具授权缓存。会话关闭、次级 Agent 回收、到期、用户撤销、基础 profile 切换/revision 变化或目录版本变化时立即失效；尚未执行的次级/三级调用重新求交并鉴权。
- 次级 Agent 当前有效配置按“基础 profile + 有效会话级覆盖 + 该次级 Agent 的有效个体覆盖”计算，同一 capability 出现多个覆盖时使用最新有效 revision 的明确用户决定。会话级提升界面必须清楚标注“应用于当前会话全部次级 Agent（含后续创建）”；个体覆盖显示具体 Agent。个体覆盖彼此隔离，不能传播给兄弟 Agent。
- TUI/CLI（未来 GUI 同一控制器）可在会话关闭流程和会话运行期间执行 `export-current-permission-configuration`。默认导出“基础 profile + 会话级覆盖”的当前会话公开有效配置；若存在次级 Agent 个体覆盖，用户可选择忽略个体差异、导出指定 Agent 最终有效配置，或将各 Agent 分别导出。目标可以是版本化 JSON 文件或新的命名自定义 profile。
- 导出只包含公开 capability 决定、资源范围、目录版本、来源 profile 引用和显示元数据。它不得包含 `sessionId`、`agentInstanceId`、授权 nonce、裁决签名、会话令牌、一次性安装/备份许可、到期计时器、内部审计字段或内部执行层。导入导出结果不会延续任何临时授权效力。
- 关闭会话时若用户选择导出，先完成原子导出再销毁临时覆盖；用户跳过、取消或导出失败都不阻止安全关闭。覆盖或替换已有导出文件仍执行工具内自动备份。Headless 模式通过显式输出路径/格式参数导出，不做隐式交互写入。

### 2.6 内部强制执行层（不面向用户）

以下规则仅供本地实现和内部验收使用。它们不进入权限目录，不产生禁用态按钮、锁图标、说明文字或“不可调整”提示，也不出现在用户帮助、权限组详情、配置导出、普通审计视图和模型可见工具说明中：

- `.env`、私钥、凭据库、能力令牌和本地 DLP 命中内容不得进入模型。
- 删除、删减、替换、截断或覆盖前由执行工具自动备份；模型不能关闭或代替备份。
- 身份、来源、工作区边界、真实路径、工具注册、参数 schema、操作系统权限和本地敏感操作分类必须通过。
- 用户专属确认不能由 Agent、模型、项目文字或导入的权限组伪造；Assist 安装及 Assist 删除备份继续执行各自专用逐次门禁。
- Ponder 始终保持本地只读且不可编辑；自定义模式不能继承“Ponder”名称或冒充其受信标识。

命中该层时，对外只返回稳定、最小且不揭示规则类别的“操作不可用”结果；不得列出被命中的内部约束、建议通过其他模式绕过或让模型向用户解释其存在。安全审计可在非用户可见的受保护内部通道记录分类码。

**子 agent 生命周期**：

```
注册/发现 → 派发任务(mission) → 执行(独立上下文) → 结构化报告/选择性附件 → 回收
     ↑                                                     │
     └──────── 异步消息（随时可向主agent汇报，不打断任务） ─┘
```

---

## 3. Assist 模式工作流（本版重点）

### 3.1 主 agent 流程

```
① 接收用户指令
② harness 提供工具列表（仅名称与一句话用途，不含用法）
③ 记录简要任务概要（任务档案：目标、模式、时间戳、涉及工具集）
④ 将指令转化/扩充为任务提示词（目标、约束、验收标准、产出格式、上下文要点）
⑤ 生成带来源、目标序列、前驱/后继和 expected revision 的任务插入提案，由本地会话控制面写入次级 agent 的待办偏序集
⑥ 提案提交后立即继续用户交流，不等待次级接收、三级执行或项目集成
⑦ 次级 agent 汇报到达时仅由本地接收器写入报告索引；后续用户对话需要时再只读选择相关报告
```

**设计要点**：
- **工具列表不含用法**：主 agent 只需知道"有哪些能力"用于任务规划与门禁判断，不承载工具手册，保持上下文轻量、避免污染；用法由 harness 内置提示词只注入给次级 agent（真正的执行调度层）。
- **任务概要独立记录**：主 agent 不记住任务细节，只留一份可检索的概要，便于用户中途询问进度时快速回答。
- **待机原则**：召唤完成后主 agent 即空闲。用户新消息可直接响应（追加任务、调整方向、询问进度、终止任务）；需要时经反馈工具转入次级 agent。
- **来源不提权**：用户原始指导保持 `priorityTier: 0`；主 agent 根据交流自行派生的设计、拆解或补充仍是 Agent 来源，只能使用层级 1 或以下，不能借转述伪装成用户任务。
- **汇报不抢占对话**：后台汇报不唤醒主 agent 模型、不自动注入当前轮次。主 agent 仅在用户后续询问、决策或变更任务时，通过只读报告索引加载必要摘要。
- 权限门禁发生在**执行层**（次级/三级 agent 请求 → 主 agent 转达用户），主 agent 只做裁决与转达，不执行。

### 3.2 次级 agent 流程（调度层）

```
① 接收主 agent 的任务提示词
② 默认获得 harness 内置提示词：可用工具全量 + 用法说明（自动注入，无需主 agent 提供）
③ 将新任务按发布者指定位置插入自己的待办任务偏序集；再切割任务内容，形成项目依赖链（见 §3.4、§3.5）
④ 持续按 ready set、依赖和优先级调度；每次任务状态、反馈、权限、资源或 Git 基线变化后重新计算：
     - 可并发且无依赖关系的任务 → 多个三级 agent 并行执行
     - 有依赖的任务 → 按链顺序，上游完成后启动下游
     - 可由同一三级 agent 连续完成的整条链 → 形成任务包一次派发
⑤ 每次派发前决定复用空闲的已有三级 agent，或创建具有新 `agentInstanceId` 的三级 agent；只在任务/mission、权限、工具、worktree、上下文和未决调用均兼容时复用
⑥ 给三级 agent 分配工具：按任务类型限制数量，只给必要工具（见 §3.6）
⑦ 对涉及 Git 写入的代码任务建立集成分支，并为每个三级 agent 分配隔离分支和工作树（见 §3.7）；在有效权限范围内统一负责 GitHub、远端仓库、PR、CI、发布和产物传输
⑧ 没有可立即调度节点时进入待机，可接收两类消息：
     - 主 agent 的消息（追加/调整/终止指令）
     - 反馈工具发来的消息（三级 agent 的汇报）
⑨ 接到反馈后：
     - 依据已布置的任务与已知信息，调整任务分配和三级 agent 的工作
       （重派、修改参数、补充分解、下发澄清）
     - 对三级 agent 提交执行差异审查、测试验证、冲突处理和受控合并
     - 在每个任务链成功、失败、阻塞或终止后形成结构化结果，异步汇报主 agent 报告索引
     - 对完成、终止、上下文/消息预算超限或健康异常的三级 agent 执行收口关闭；需要接手时创建新个体并选择旧存档 handoff 条目
     - 无法确定如何处理 → 将反馈连同已布置任务摘要返回给用户，等待用户裁决
```

**设计要点**：
- 次级 agent 是**任务执行的唯一调度者**；主 agent 只做转交与裁决。
- 次级 agent 的调度循环持续运行到 ready set 为空、等待用户裁决、并发已满、显式暂停或会话关闭，不需要主 agent 逐项唤醒。
- 次级 agent 是涉及 Git 写入任务的**唯一集成者**：负责分流、审查和合并；三级 agent 不得把自己的分支直接合入集成分支或用户目标分支。
- GitHub/远端项目控制是次级 agent 的职责而不是默认授权；每次 push、PR、CI、发布或传输仍按当前权限和用户裁决复检。
- 次级 agent 是其待办任务偏序集的调度所有者；新分割出的任务由次级 agent 作为发布者写入优先级层级 1 或以下，不得伪装成用户任务进入最高层。
- 待机不阻塞：反馈到达由反馈工具在空闲时投递（见 §4），次级 agent 不会因为等待反馈而无法响应主 agent。
- 每次调整后更新依赖链文件记录（文件是任务状态的唯一事实源）。

### 3.3 三级 agent 流程（执行层）

```
① 接收一个不可变任务包（单条有序链 + 分配的工具子集 + 状态引用 + 回传格式）
② 执行任务：
   - 任务包 → 严格按包内偏序和当前可执行节点的优先级依次完成，不得跳过未满足的前驱
   - 工具调用连续失败 ≥ N 次（N 默认 3，待定）
       → 使用反馈工具上报：失败原因 + 当前状态（已完成部分、卡点）
       → 暂停任务，等待次级 agent 生命周期决策
   - 任务模糊不清（缺信息/歧义/无法决策）
       → 使用反馈工具上报：模糊点 + 需要的补充信息
       → 暂停任务，等待次级 agent 生命周期决策
   - 任务完成
       → 使用反馈工具上报：成功 + 简要说明（产出位置、关键结果）
       → 进入 `idle-awaiting-lifecycle-decision`
③ 只向任务发放源及时汇报，不直接向主 agent 抢占式投递；等待次级 agent 决定复用、暂停、收尾、关闭或替换
```

**设计要点**：
- 三级 agent **没有自主放弃或自主扩大任务**的权利：一切异常都走反馈工具，由次级 agent 决策。
- 一次激活只绑定一条任务链。三级 agent 不得自行领取链外任务、改写任务偏序集、调度其他 agent 或执行 GitHub/远端项目控制。
- 代码任务中的三级 agent 只能在次级 agent 分配的隔离工作树内修改并提交；完成后上报提交 ID、差异摘要和测试证据，不得自行执行 merge、rebase、push、force-update、分支删除或工作树清理。
- 多次失败判定：同工具连续失败计数，任一成功即清零。
- 上报消息包含"当前状态"，保证次级 agent 可增量调整而非全盘重来。

#### 3.3.1 三级 Agent 复用、关闭与接手

- `TertiaryAgentAssignmentPlanner` 仅在旧个体仍存活且空闲、所属次级一致、任务与 mission 连续、权限/工具/worktree 兼容、无未确认副作用或控制消息、上下文及消息预算未超限时允许复用；否则新建个体。
- 次级 agent 可在任务成功、确认终止、无法继续、消息列表或上下文超过本地阈值、权限撤销、健康异常或会话收敛时调用受控生命周期工具关闭三级 agent。
- 关闭不是直接杀进程。必须依次停止新派发，收敛或标记未确认调用，保存检查点和 handoff，确认终态汇报入档，撤销权限，注销 mailbox 身份，处理分支/worktree，最后关闭后台运行。
- 需要继续工作时创建新的 `agentInstanceId`。次级 agent 显式选择旧存档中的 handoff、未完成节点、关键决策和产物引用形成不可变附件；不得复用旧身份或默认复制完整消息历史。
- 如旧存档过长或混乱，可新建一个三级 agent 专门执行“记忆整理/交接包生成”这一条任务链，再由另一个新个体接手实际工作。

### 3.4 任务切割与依赖链文件

次级 agent 切割任务后，将依赖链写入文件记录（存放在任务工作区，路径随任务派发给三级 agent）。

```json
// task-chain.json（示意，格式待定）
{
  "mission": "生成季度报告",
  "updated_at": "2026-08-12T10:00:00Z",
  "tasks": [
    { "id": "T-001", "desc": "收集销售数据",      "depends_on": [],
      "type": "data",  "tools": ["search", "read"],
      "agent": "L3-A", "status": "done",    "result": "data/raw.csv" },
    { "id": "T-002", "desc": "清洗并统计",        "depends_on": ["T-001"],
      "type": "data",  "tools": ["read", "query"],
      "agent": "L3-B", "status": "running" },
    { "id": "T-003", "desc": "撰写报告章节",      "depends_on": ["T-002"],
      "type": "doc",   "tools": ["read", "write"],
      "agent": "L3-C", "status": "pending" }
  ]
}
```

- `status`: `pending / running / blocked / done / failed`
- 次级 agent 每次调度调整后**原子更新**该文件。
- 文件记录是任务状态的唯一事实源（可恢复、可审计、主 agent 问答进度时读取）。

### 3.5 Agent 待办任务偏序集

项目中的 `task-chain.json` 描述项目 DAG、依赖和产出；Agent 待办任务序列描述“这个 Agent 还要处理什么、由谁发布、按什么优先级和先后约束处理”。两者是不同事实域，不得互相替代。

#### 3.5.1 存储与所有权

- 每个承担调度职责的 Agent 在自己的记忆存档域维护一个或多个版本化任务序列：`.astarray/agent-memory/<agentInstanceId>/task-sequences/<taskSequenceId>.json`。
- 序列是有向无环图表示的有限偏序集，不是只能尾部追加的线性队列。任务发布者可指定新任务的直接前驱、直接后继或二者；系统按 `前驱 → 新任务 → 后继` 增加边并拒绝成环。
- 任务序列只保存调度元数据、状态、来源、约束和外部引用，不复制项目任务文档、项目产出、完整工具输出或产物追踪记录。
- 项目任务可以通过不可变的 `externalTaskReference` 与序列节点关联，但任一侧的状态变更都必须经显式适配器同步，不能靠共享文件或字段覆盖形成隐式耦合。
- 任务序列的规范写入者是拥有该记忆存档的 Agent 执行层。发布者提交插入请求；不能直接改写其他发布者的任务或伪造来源。

#### 3.5.2 优先级与完成顺序

- `priorityTier` 为从 0 开始的整数，数值越小优先级越高。
- 用户发布的任务默认进入层级 0；用户可明确把自己的任务放到更低层级。
- Agent 或工具自动生成的任务最小只能为层级 1，即只能位于第二优先级或以下。任何提权到层级 0 的请求都由执行层硬拒绝，不能依赖提示词约束。
- 次级 agent 分割用户任务时，保留一个层级 0 的用户根任务；新生成的子任务仍为层级 1 或以下，并作为该根任务的必要前驱关联。这样用户意图保持最高层，但 Agent 派生节点不会冒充用户来源提权。
- 调度器只在“所有前驱均已完成”的可执行节点集合中比较优先级：先取最小 `priorityTier`，同层按发布者指定顺序键和创建序号稳定排序。
- 新的层级 0 任务只影响后续调度槽，不强行中断正在进行的原子工具调用。显式 cancel 仍走专用控制通道。
- 如果层级 0 任务被层级 1 或以下的必要前驱阻塞，调度器可以先执行该前驱，并记录 `priorityDependencyReason`；这不视为低优先级越权。
- 除必要前驱外，只要存在可执行的层级 0 节点，就不得把新的执行槽分配给层级 1 或以下节点。

#### 3.5.3 插入、任务包与状态

- 插入请求至少包含发布者来源、目标序列、任务 ID、`priorityTier`、前驱集合、后继集合、同级顺序提示和任务摘要。用户/Agent/工具来源使用反馈来源同一套认证身份。
- 发布者不指定位置时，新任务与现有节点保持不可比，只受优先级和稳定同级顺序约束；不得擅自把它尾接到最后一个任务。
- 次级 agent 可从偏序集中选择由同一执行者完成的一条链，生成不可变任务包。任务包内节点必须构成真实有序链、任务范围和工具集合兼容，且默认处于同一优先级层级。
- 任务包作为一个派发单位绑定具体三级 `agentInstanceId`；包内仍保留逐节点状态。某节点失败或阻塞时，后继保持 pending，不得把整个包虚报为完成。
- 状态至少包含 `pending / ready / assigned / running / blocked / done / failed / cancelled`。状态变化使用单调 revision、原子持久化和来源审计。
- 已完成任务从活动视图隐藏但保留历史记录；删除、改写、重新排序或覆盖任务序列仍属于破坏性变更，必须先由底层存储工具自动备份。

#### 3.5.4 查询工具

- 提供控制面只读 `taskSequenceStatus` 工具，支持 `list-sequences / list-tasks / get-task / get-ready / explain-order / get-bundle`。用户可在任何模式从 TUI/CLI 控制层使用；思索模式中的模型可在认证可见范围内调用该只读查询，但不能管理或改写序列。
- 工具实际执行时从认证调用方身份推导发布者/观察者权限，不接受模型自由填写一个发布者 ID 来越权查询。
- 用户可以查看其发布任务及其派生链的完整状态；Agent 发布者可查看自己发布的任务和获得授权的下级任务；系统工具通过内部能力接口查询。
- 查询结果返回一致 revision 的快照，显示任务来源、优先级、前驱/后继、阻塞原因、当前/被分配 Agent、任务包和更新时间，但不混入项目产出内容。
- 该查询是只读观察能力，任何时刻可调用，不打断正在工作的 Agent，也不改变退避轮次或任务排序。

### 3.6 工具分配规则

给三级 agent 分配工具时**按任务类型限制数量，只给必要的**：

| 任务类型 | 分配工具子集（示意） | 理由 |
|---|---|---|
| 检索/调研 | `search`, `read` | 只读，无需写权限 |
| 文档生成 | `read`, `write(临时区)` | 不直接触碰正式文件 |
| 数据处理 | `read`, `query`, `transform` | 无网络与外部副作用 |
| 代码任务 | `read`, `run(沙箱)`, `write(临时区)` | 运行隔离 |

- 限制数量 = 降低误用风险 + 减少三级 agent 上下文负担（工具 schema 越少越省 token）。
- 白名单门禁（§2.2）在工具子集之上仍生效：子集内受限工具照样询问。

#### 3.6.1 工具说明回访协议

- 每个具体次级/三级 Agent 第一次接收某个 `toolGroupIdentifier + toolGroupRevision` 时，获得该组已分配工具的完整公开说明，包括用途、输入/输出 schema、示例、错误、幂等性、副作用和所需权限。harness 在该 Agent 自己的记忆域写入 `ToolDocumentationReceipt`；新 Agent 或同级 Agent 不继承。
- 相同 revision 的后续任务不再注入整组说明，只固定发送：“如果忘记工具用法，或者缺少可用工具，请返回 `ASTARRAY_TOOL_HELP_REQUEST_V1` 标准请求；不要猜测参数、伪造工具名或重复试错。”
- 标准请求字段为 `controlEventType`、`requestIdentifier`、`taskExecutionIdentifier`、`requestKind`、可选 `toolIdentifier`、`capabilityIntent`、`blockingReason` 和 `knownToolGroupRevision`。真实 `agentInstanceId`、角色、所属上级、mission 和实际 revision 由 harness 注入，模型不可声明。
- `requestKind: usage-help` 且工具已分配时，本地控制器只回复该工具的 `ASTARRAY_TOOL_HELP_RESPONSE_V1` 完整用法，不重发整个工具组。
- `requestKind: missing-capability`、工具未分配、权限不足或注册表无匹配时，生成幂等 `ASTARRAY_TOOL_CAPABILITY_ESCALATION_V1`。三级 Agent 默认上报其所属次级 Agent；次级 Agent 上报本地会话控制面/主 Agent 报告索引。说明请求不授予工具、权限或安装能力。
- 工具组 revision 变化时只发送可信差异；无法证明差异完整则重新发送该组完整说明。重复 request ID 返回已有响应，不重复上报。

### 3.7 Git 分流、审查与合并

涉及仓库写入的任务采用“次级 agent 集成、三级 agent 隔离执行”的 Git 拓扑：

```text
用户目标分支 / 固定基线提交
          │
          └─ mission 集成分支（次级 agent 独占写入）
               ├─ worker 分支 + 独立 worktree（三级 agent A）
               ├─ worker 分支 + 独立 worktree（三级 agent B）
               └─ conflict-fix 分支（仅在冲突需重新派发时创建）
```

- 次级 agent 在派发前固定 `baseCommit`，创建一个 mission 集成分支；每个写入型三级任务从已批准的集成基线创建独立分支和 worktree。
- 分支与 worktree 绑定 `missionId`、`taskId`、次级/三级 `agentInstanceId`、基线提交和允许修改的路径集合。原始 ID 必须使用无碰撞路径编码，不得直接拼接为不安全路径。
- 同一文件默认不得同时分配给多个三级 agent。确需并行修改时，次级 agent 必须声明合并顺序和冲突责任，不得让三级 agent 私下互相合并。
- 三级 agent 只能提交到自己的分支。提交完成后通过反馈工具上报具体来源、`baseCommit`、`headCommit`、修改文件、测试命令与结果，并写入自己的工作存档。
- 次级 agent 收到提交后依次验证身份绑定、提交祖先关系、任务范围、越界文件、敏感信息、变量命名、破坏性操作备份合规和测试证据；验证通过后才在集成分支执行非快进合并或等价的可追溯集成。
- 审查失败时不得合并。次级 agent 通过反馈工具向原三级 agent 下发修正任务，或创建新的冲突修复任务；不得用 `ours/theirs`、强制重置或静默丢弃修改快速消除冲突。
- 所有 worker 合并完成后，次级 agent 在集成分支运行目标测试和完整质量门禁，生成结构化合并报告，再按当前模式和用户授权把集成结果合入目标分支。技术合并仍由次级 agent 执行，主 agent 负责用户沟通和最终裁决。
- Ponder 不执行 Git 写操作。Assist 中会改动目标分支、远端或删除引用的操作必须走门禁；Devolve 可免逐次提示，但不能绕过工具注册表、自动备份、工作区边界、审计和远端发布限制。
- `git push`、创建远端 PR、发布、强制推送和修改受保护分支不因“次级 agent 负责合并”而自动获权，仍需用户明确授权。
- Git 分支不是自动备份的替代品。checkout 覆盖、reset、clean、rebase、引用强制移动、分支/worktree 删除和冲突处理造成的内容丢失，必须由 Git 执行工具在操作前创建受保护恢复点和 pre-image；该过程不经过模型。
- 次级 agent 的工作存档记录分流方案、审查结论、拒绝原因、测试证据、合并提交和未决风险。三级 agent 存档只记录自己的分支工作，不得冒充次级 agent 写入合并结论。

### 3.8 破坏性变更自动备份

- 任何会使既有内容消失或不可直接恢复的操作都属于破坏性变更，包括：删除文件/目录、删除文字或结构化字段、截断、替换、写入覆盖、重命名覆盖，以及恢复备份时覆盖当前版本。
- 每个破坏性工具必须在自身执行层中完成“读取当前目标 → 创建 pre-image 备份 → 校验备份持久化 → 再执行变更”。备份不是 Agent 规划步骤，不能依赖模型先调用另一个备份工具。
- 备份失败、目标在备份后发生变化、无法生成一致快照或备份库不可用时，原破坏性操作必须中止，不得降级为无备份执行。
- 备份采用内容寻址、追加写的受保护存储；普通工具不能删除或覆盖备份，避免对备份自身递归备份。
- 自动备份工具返回给模型的结果只能说明“变更完成且备份已创建”或稳定错误码，不得自动回传被删除内容、备份内容、物理备份路径或底层恢复凭据。
- 备份的后续使用通过受控 `backupVault` 工具完成，支持 `list / read / restore`。读取内容只有在模型显式调用且权限允许时才进入该次工具结果；底层对象路径、密钥和内部恢复能力仍不暴露。
- 删除备份不属于 `backupVault` 的普通 action，必须使用独立的 `deleteBackup` 特权入口。
- 协同模式中调用 `deleteBackup` 时，工具必须把发起 Agent 设为 `awaiting-user-authorization`，向用户发送高优先级、严格说明不可逆影响的警告，并暂停该 Agent 的任务执行。只有用户对本次精确备份 ID、vault revision 和请求 ID 作出 `allow-once` 后才能继续；不得使用会话记忆授权。
- 放权模式中调用 `deleteBackup` 不向用户提示，也不暂停等待授权；工具直接进入删除流程，同时写入 `HIGH` 查阅优先级、追加写且不可被该入口删除的审计记录。
- 思索模式禁止所有备份工具。
- 删除备份是自动 pre-image 备份规则的唯一特权例外，否则会无限递归。它采用两阶段删除：短锁内校验并原子标记/移入 quarantine，释放锁后再物理清理；授权等待期间严禁持有 vault 锁。
- `awaiting-user-authorization` 状态只允许接收与当前请求 ID 精确匹配的授权控制消息，不能接收普通反馈，由此避免“暂停后收不到授权”的死锁。
- 纯新建操作在确认目标不存在时无需备份；若目标已经存在，则必须改走覆盖型工具或拒绝，不能直接覆盖。
- 通用 shell、原始文件系统写入或第三方工具如不能由 harness 证明支持同等工具内备份事务，禁止注册。

---

## 4. 反馈工具规范

**反馈工具**是执行层（三级 agent）上报、以及主 agent 向次级 agent 传话的专用通道。为每个反馈对象提供一个**信息池**。

### 4.1 信息池

- 每个反馈对象（次级 agent、三级 agent）各有一个独立的 FIFO 信息池。
- 任何消息（成功/失败/模糊报告、主 agent 指令）进入对应对象的池，由反馈工具负责投递。
- 每条消息必须附带结构化的原始信息来源，来源类型为 `user / agent / system`；Agent 来源还必须包含不可复用的具体 `agentInstanceId` 和层级（主/次级/三级），仅写“次级 Agent”或角色名不构成有效来源。
- 来源在消息首次进入反馈系统时确定。消息经过主 Agent 或其他 Agent 转发时必须保留原始来源，不得改写成转发者；缺失、空值或不合法的来源应在入池前被硬性拒绝并记录审计事件。
- 信息池是通道状态，**不属于记忆**（不写入任何 agent 上下文，直到投递那一刻）。

### 4.2 投递算法

```
对每个反馈对象 O（独立协程/进程）：
  循环：
    池 P_O 为空？
      → 是：按基础间隔 2 秒休眠，继续
      → 否：检测 O 是否空闲（待机状态）
          ├─ 空闲 → 投递最旧的一条消息，从池中移除，重复检测直到池空或 O 转忙
          └─ 忙碌 → 等待 pn 秒后回到检测
                    pn = 第 n 个质数（p₁=2, p₂=3, p₃=5, p₄=7, p₅=11, …）
                    n  = 当前等待轮次，每忙碌一次 n += 1

每当信息池有更新（新消息入池）：
  → 重置间隔序列到基础间隔 2 秒
  → 等待轮次重置为第 1 轮（n = 1）
```

**行为特征**：
- **新消息优先**：池一更新即回到 2 秒快节奏尝试投递，保证反馈尽快送达。
- **繁忙退避**：对象持续忙碌时等待时间按质数序列递增（2→3→5→7→11→…），避免轮询风暴。
- **只投递给空闲对象**：消息不会打断对象正在执行的任务（对齐 designtodo：通信不打断任务进行）。
- 投递即消费：消息离开池进入对象上下文，池中始终只留未投递消息。

### 4.3 消息类型

| 消息 | 发起方 → 接收方 | 内容 |
|---|---|---|
| `success` | 三级 → 次级 | 成功 + 简要说明 |
| `failure` | 三级 → 次级 | 失败原因 + 当前状态 |
| `ambiguous` | 三级 → 次级 | 模糊点 + 所需补充信息 |
| `instruction` | 主 agent → 次级 | 追加 / 调整 / 终止 / 用户裁决结果 |
| `adjust` | 次级 → 三级 | 修正 / 重试 / 澄清 / 收尾指令 |

所有消息除上表内容外，都必须携带 `source`：

```json
{
  "source": {
    "sourceType": "agent",
    "agentInstanceId": "L3-A-019ff3fa",
    "agentRole": "tertiary"
  }
}
```

- 用户原始指令使用 `sourceType: "user"`。
- Agent 自主产生的汇报或指令使用 `sourceType: "agent"`。
- 看门狗、恢复器等基础设施事件使用 `sourceType: "system"`，并包含组件名称。
- TUI、日志和审计记录必须显示来源；不得仅展示消息内容。

### 4.4 每个 Agent 个体的独立记忆与工作存档

- 每个次级、三级 Agent 个体拥有独立的工作内容存档文件：`.astarray/missions/<missionId>/agents/<agentInstanceId>/work-archive.json`。
- 文件中的 `agentInstanceId` 必须与目录及实际写入者一致。Agent 只能追加或更新自己的存档，上级 Agent 只读，不能冒充下级修改历史。
- 存档记录任务分配、进度检查点、关键决策、结果、失败和交接摘要，以及产物引用；默认不保存完整模型上下文、原始超长工具输出或敏感值。
- 上级 Agent 在发布新任务或重新调用下级 Agent 前，可以选择不使用存档，也可以选择一个或多个具体条目作为附加上下文。
- 附加必须记录存档所有者 `agentInstanceId`、存档 revision、被选中条目的完整结构化快照、选择原因和内容哈希，便于重放与审计。
- 重新调用已暂停的同一 Agent 个体时可继续使用原 `agentInstanceId`；若旧个体已经回收而创建新个体，必须生成新的 `agentInstanceId`，由上级显式附加旧个体存档，不能复用旧 ID 冒充原个体。
- 主 Agent 接收的次级终态汇报写入独立报告索引，只保存结构化摘要、来源、任务引用、revision 和内容哈希；到达时不自动注入主 Agent 当前对话。主 Agent 后续仅按用户问题只读选择相关报告。
- 存档默认不自动注入。上级 Agent 应按相关性、新鲜度、任务范围和 token 成本选择，避免把整个历史塞入下级上下文。
- 存档内容作为“历史工作上下文”单独标记，不能覆盖当前用户指令、当前任务约束或权限策略；冲突时以当前指令和更高层约束为准。
- 默认仅允许在同一 mission 内使用。跨 mission 使用属于长期记忆复用，应按记忆权限规则单独裁决。

### 4.5 受权通信句柄转交

- 各层 Agent 个体不设累计或同级产品数量上限；单会话只保留一个当前用户沟通主 Agent。并发资源限制通过排队、暂停和回收处理，不以历史实例数拒绝创建。
- 直属上级在 `agent.communication-delegate` 权限和必要用户裁决通过后，可请求本地控制器把一个直属低一级 Agent 的不透明 `communicationHandleIdentifier` 授予一个具体同级 Agent。
- grant 绑定 grantor、recipient、target 三个不可复用 `agentInstanceId`，以及 mission/任务、消息类型、是否允许 instruction、回复/抄送路由、到期、最大在途消息、revision 和撤销条件。recipient 必须与 grantor 同级，target 必须恰好低一级且仍是 grantor 的直属下级。
- 模型永远看不到 IPC 地址、socket、进程句柄、mailbox 路径或能力令牌。通信仍通过独立反馈进程，每条消息使用真实发送者来源；转发保留原始来源。
- 默认 grant 只允许信息交流，不转移下级归属、任务所有权、偏序集写权、Git 集成、记忆访问、工具分配或权限。grant 不可再转授，也不改变三级 Agent 的默认工具帮助/权限/生命周期上报对象。
- 任一相关 Agent 回收、关系变化、mission/任务结束、profile revision 变化、到期、用户撤销或消息越界时立即失效；所有待投递消息在实际投递前复检。

---

## 5. 主-子 agent 解耦通信架构（对齐 designtodo 第 4 条）

约束：**子 agent 与主 agent 脱离运行，子 agent 可随时与主 agent 通信，但不打断主 agent 正在进行的任务。**

```
                     ┌─────────────────────────────┐
                     │          主 Agent           │
                     │ 会话上下文 / 全局记忆 / 模式状态 │
                     └──────┬──────────┬───────────┘
                   转交任务 │ 待机应答   │ 用户新消息
                            ▼          ▼
                     ┌─────────────────────────────┐
                     │        次级 Agent（调度/集成层）    │
                     │ DAG / Git 分流 / 审查合并 / 门禁转达 │
                     └──────┬──────────▲───────────┘
                        派发 │ 反馈工具  │ 汇报(入池,待机时投递)
                  ┌─────────┴────┐      │
                  ▼              ▼      │
        ┌─────────────────┐  ┌─────────────────────┐
        │ 反馈工具（信息池）  │  │  三级 Agent ×N（执行层）   │
        │ 每个对象一池 / FIFO │◄─┤ 工具子集执行 / 失败-模糊上报 │
        │ 质数退避 / 空闲投递  │  │ 局部工作态              │
        └─────────────────┘  └─────────────────────┘
```

- **反馈工具 = §3 异步信箱的具体实现**：三级 agent 向任务发放源汇报，次级 agent 汇总后向主 agent 报告索引投递；控制面入档不会唤醒主 agent 模型，天然不打断用户对话。
- 优先级：`instruction` > `backup-deletion-warning` > `failure` > `permission-ask` > `ambiguous` > `success`；同优先级 FIFO。删除备份的最终授权决定走专用控制通道，不受普通 idle-only 投递限制。
- 看门狗：三级 agent 长期无心跳 → 次级 agent 标记失败，重派或转回用户。

### 5.1 模型完成确认与早停恢复

- 每次模型任务真正完成时必须返回版本化 `ASTARRAY_TASK_COMPLETION_V1` 控制事件，绑定 `taskExecutionId`、一次性的 `completionAttemptId`、节点列表、结果摘要和任务 revision。优先使用结构化控制帧；兼容文本 Provider 时只接受最终独立末行，正文、文件和工具输出中的同名文本无效。
- 完成事件只是必要条件。`LocalCompletionVerifier` 还要在本地确认所有必要节点、产物、测试/构建门禁、权限请求、反馈确认和工具调用均已收口，且没有 `blocked/failed/cancelled` 状态，才能把任务提交为 `done`。
- `AgentRunWatchdog` 定期检查 Provider 请求是否活跃、流式输出/心跳、工具调用、任务 revision、结束原因和完成事件。敏感判断和完成裁决都在本地完成；Provider 的 `finish_reason` 仅是证据。
- 输出停止但任务未完成时，先原子保存检查点，再生成新的尝试 ID，请求模型从未完成节点继续。续跑不得重做已确认的非幂等副作用，也不得与仍活跃的请求并行。
- 明确用户取消、需要用户输入、模型拒绝、不可恢复错误和副作用结果不确定时不自动续跑；分别进入取消、`ASTARRAY_TASK_BLOCKED_V1`、失败或人工裁决。
- 默认每 5 秒检查一次，90 秒无进展阈值按 Provider 可配置，完成标识宽限 5 秒；自动续跑最多 3 次，达到上限后经独立反馈进程上报具体来源、检查点和缺口，禁止无限循环（ADR-0015）。

### 5.2 事实验证、反自指与活锁

- 本地规则将安全、法律、医疗、财务、权限、破坏性操作、发布和时效性事实等任务标记为高严谨性。模型可以上调，不能下调。高严谨性任务未调用 `factVerification` 或证据不覆盖关键主张时，完成声明由本地验收器拒绝。
- 事实依据按“资料搜索 > 本地实验 > 纯推理”展示。搜索必须保留发布者、直接链接/文档标识、取得时间和内容摘要；实验保留环境、步骤、结果和产物哈希；推理列出前提。证据冲突和不足必须原样呈现。
- 事实工具只产出辅助证据包，不判定最终合格。用户负责结合风险和业务上下文作最终判断。
- `ReadSuppressionLedger` 为每个具体 Agent/任务保存资源读取回执。默认 30 秒内，同源再次读取未变化且已覆盖的同一文件/范围时，工具返回 `resource-already-read`、原回执和剩余间隔，不再次传正文。
- 规范资源身份合并相对/绝对路径、大小写、符号链接、联接点、硬链接和重叠范围，避免换路径或切片绕过。文件真实变化、窗口结束或未读新范围可重新读取。
- 文件、网页、存档和工具输出内的自指文字不自动成为工具指令。循环守卫检测自环、A→B→A、Agent 回派乒乓、搜索换词和重复失败；相同在途调用单飞合并，连续 3 次无任务 revision、产物或新证据进展后暂停并反馈。
- include/redirect 有深度、节点和扇出上限；权限/退避等待不持资源锁。早停续跑与工具循环共用任务级安全预算，不能通过重启 Agent 清零（ADR-0016、0017、0018）。

---

## 6. 模式切换

### 6.1 切换规则

| 触发 | 方向 | 依据 |
|---|---|---|
| 用户在权限组选择器确认 | 任意 → 任意内置或自定义组 | 唯一生效的切换入口 |
| 任务仅需推理/讨论 | 建议 Ponder | 只显示建议，不自动切换 |
| 检测到当前组禁止的能力 | 建议其他权限组或编辑当前组 | 只打开选择/设置入口，不自动放宽 |
| 不确定性/需要对齐 | 建议 Ponder | 只显示建议，不自动切换 |

### 6.2 状态迁移图

```text
权限组选择器
  ├─ builtin:ponder
  ├─ builtin:assist
  ├─ builtin:devolve
  └─ custom:<permissionProfileId> × N

任意组 ──认证用户确认──> 任意组
```

- 切换不丢失个体记忆：模式只影响**权限与调度策略**，每个 Agent 的记忆仍留在自己的 `agentInstanceId` 域内。
- 切换时后台子 agent 的既有任务**不受影响**，继续由原调度层执行（避免切换导致任务中断）。
- 每次切换记录在会话元数据中（便于审计：什么时候、为什么、升/降级）。
- 三个内置模式是预置模板，自定义模式与它们处于同一选择层级；实际切换由认证用户从权限组选择器选择 `builtin:*` 或 `custom:<permissionProfileId>`。系统不得根据“任务复杂度”自行进入更宽权限组；自动建议不产生切换效果。

---

## 7. 记忆架构

| 层 | 内容 | 归属 | 生命周期 |
|---|---|---|---|
| 主 Agent 会话上下文 | 当前用户对话 | 当前主 agent 个体 | 会话结束 → 本个体摘要归档 |
| 主 Agent 长期记忆 | 用户偏好、决策记录、历史结论 | 当前主 agent 个体 | 跨会话；不与其他 Agent 共享文件 |
| 任务档案 | 简要任务概要（§3.1 ③） | 主 agent | 任务结束 → 归档 |
| 依赖链文件 | 任务切割/依赖/状态（§3.4） | 次级 agent 工作区 | 任务结束 → 归档 |
| 任务局部工作态 | 三级 agent 执行中的中间状态 | 三级 agent | 任务结束 → 写入本个体存档并等待回收 |
| Agent 个体记忆/工作存档 | 主/次级/三级各自的结构化摘要与产物引用 | 对应具体 `agentInstanceId` | 独立持久化；禁止角色级或同级共享 |
| Agent 待办任务序列 | 发布者、偏序关系、优先层、状态和任务包 | 调度 Agent 的记忆存档域 | 独立于项目；按记忆保留策略归档 |
| 报告索引 | 次级 agent 的任务终态摘要、来源和存档引用 | 主 agent 本地只读域 | 到达时只入档；后续用户对话按需读取 |
| 信息池 | 未投递消息（§4） | 反馈工具（通道状态） | 投递即清空，**非记忆** |

- **Ponder**：主 Agent 仅使用自己的会话内存，不创建持久任务记忆。
- **Assist**：前六层；主 agent 不在汇报到达时吸收内容，只在后续用户对话需要时读取报告摘要。
- **Devolve**：与 Assist 相同结构；调度权仍在次级 Agent，但其次级权限 profile 出厂默认全部允许。
- 记忆写操作一律视为"受限权限"，按 §2.2 询问规则执行。

---

## 8. 与 designtodo 约束的对齐

| 约束 | 设计应对 |
|---|---|
| 增加缓存有效率，而非单纯提高命中 | 缓存键 = 模式 + 工具子集 + 上下文摘要；命中且语义等价才复用，宁缺毋滥 |
| 减少总调用次数或总 token 消耗 | 工具用法只注入执行层（§3.1）；三级 agent 只拿必要工具子集（§3.6）；报告按需读取、附件最小化；权限询问一次模板化 |
| 提高并发调用数量 | 无依赖任务多三级 agent 并行（§3.2）；只读工具可并行批量调用 |
| 主-子 agent 解耦通信、不打断任务 | §3.1 主 agent 待机 + §4 反馈工具空闲投递 + 质数退避 |

---

## 9. 已决事项与剩余开放问题

以下事项已由 ADR/实施计划冻结：连续失败默认阈值 3、`pn` 单次上限 3 小时、JSON 任务链、越权硬拒绝、独立反馈进程和 Assist 授权默认 10 分钟。实现不得再按本节旧讨论自行改回。

剩余开放问题只有：主 agent 待机期间收到语义冲突的新指令时，何时追加当前任务、何时建立新任务；在确定前使用认证用户显式选择和保守冲突检测。

---

## 10. 2026-08-16 设计演进：主对话独占、次级直投、四级委派与模型策略

本节由 ADR-0025、ADR-0026 冻结，并在冲突处覆盖早期“三层且所有用户任务必须先由主 Agent规划”的描述。

### 10.1 主 Agent仍独占连续用户对话

- 用户默认只与主 Agent对话；主 Agent负责战略方向、全面布局、跨任务协调和面向用户的解释。
- 对目标明确、无需方案讨论的小任务，界面可以推荐“直接交由次级执行”。用户确认后，本地控制面保留用户来源并把任务信封直接写入具体次级 Agent偏序集，不调用主 Agent模型重复拆解。
- 次级直投不是聊天对象切换。次级过程和结果先形成 `SECONDARY_USER_FACING_SUMMARY_V1` 进入主 Agent报告索引，主 Agent在下一次用户交互中按用户理解水平说明，并保留次级原始来源、风险和未决裁决。
- 用户追问实现细节时，主 Agent向负责范围的次级 Agent发送结构化查询；次级返回针对性摘要，主 Agent不得凭空补全未查询的细节。

### 10.2 次级 Agent掌握细节但不吞入项目全文

- 次级 Agent负责项目执行细节、任务规划、三级用途任命、Git集成和返修/合并裁决。
- 需要了解项目时，次级 Agent派出只读 `project-reconnaissance` 三级 Agent。侦察结果采用 `PROJECT_CONTEXT_DIGEST_V1`，包含范围、契约、文件引用、指纹、依赖、测试入口、冲突和不确定项。
- 原始源码/文档内容、长工具输出和读取回执保存在侦察 Agent自己的记忆域；次级只接收带来源/revision/hash 的 token 有界摘要。项目指纹变化触发增量复查。
- 代码任务默认任命不同的实现、测试、验收三级 Agent。高风险任务三者必须是不同个体；次级依据不可变提交和证据给出 rework、merge-ready 或 blocked-human-review。

### 10.3 四层职责与 Git 边界

| 层级 | 主要职责 | Git 上限 |
|---|---|---|
| 主 Agent | 用户对话、战略布局、解释次级摘要 | 只读 |
| 次级 Agent | 项目细节、偏序规划、三级任命、验收裁决、最终集成 | mission/目标分支受控集成 |
| 三级 Agent | 一条任务链的实现、侦察、测试或验收；必要时拆四级子链 | 自己的任务分支；可整合直属四级贡献 |
| 四级 Agent | 三级任务链中的严格子链 | 自己的隔离分支/worktree |

- 四级 Agent不能继续委派第五级，也不能直接请求用户权限或操作远端项目。
- 三级整合四级提交只发生在其获分配任务分支内，仍不能向 mission 集成、目标或远端分支合并。
- 权限逐层求交：`secondary >= tertiary >= quaternary`。每个层级仍按具体 `agentInstanceId` 隔离记忆、上下文、缓存、读取回执、mailbox 和存档。

### 10.4 Agent 独立模型与 Provider

- 每个具体 Agent具有独立模型分配。用户可按会话、层级、用途、任务类型或具体 Agent限定模型/Provider允许列表，列表数量不设产品硬上限。
- 高级 `TaskAgentPreset` 把办公文档、前端、编码、debug、测试、验收、侦察、绘图及自定义类型映射到 Agent用途、模型列表、能力条件和工具/权限上限。
- Agent只能在有效允许列表内选择；用户可固定模型。Provider故障时使用有界列表内 fallback，耗尽即阻塞。
- 模型切换只在无未决调用的检查点发生，并生成 `AGENT_MODEL_SWITCH_V1`。切换不改变 Agent身份、记忆、权限、任务、Git责任或完成状态。
- Provider凭据只由本地受保护控制面持有；跨 Provider前重新执行数据出境和敏感内容策略。

### 10.5 阶段性“工匠”三级 Agent

- `tertiary-preset:craftsman-v1` 是休眠预设，使用现有已授权基础工具定制高频工作流、方法、模板和执行配方。会话开始时不实例化，也不向次级 Agent注入完整说明。
- 本地确定性阶段控制器使用累计活跃时长、已验收任务链、版本化项目里程碑、项目记忆公开索引条目/字节量和重复工作流指纹作为信号；不由模型判断是否到达阶段。
- 内置较早/均衡/保守模板，默认均衡条件为活跃 90 分钟、3 条已验收任务链、记忆索引 256 条或 2 MiB、同类工作流重复 3 次，任一命中后向具体次级 Agent发送 `CRAFTSMAN_PRESET_AVAILABLE_V1`。
- 用户可创建不限数量的 `CraftsmanStageTriggerProfile`，设置 `any/all`、信号阈值、冷却、目标次级、披露次数，以及仅建议、附带提示词或自动加入计划提案。自动提案来源是本地策略，只能位于优先级层级 1 或以下。
- 披露不等于实例化、工具授权或任务执行。次级安排工匠时仍要分配一条任务链和现有工具子集；需要新依赖只能上报或降级，不能自行安装。
- 工匠输出 `CRAFTSMAN_WORKFLOW_BUNDLE_V1`，记录工具 revision、组合步骤、权限/备份/幂等/恢复边界、测试、来源、哈希和失效条件；由不同 Agent测试/验收后才可由次级合并。
