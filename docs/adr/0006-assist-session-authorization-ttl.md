# ADR-0006：Assist 会话授权 10 分钟 TTL

- 状态：已接受（冻结）
- 日期：2026-08-12

## 背景

Assist 门禁中"会话内记住权限"需要明确有效期，避免权限长期悬挂。

## 决策

Assist 会话授权默认 10 分钟或会话结束，以先到者为准。参数变更后必须二次鉴权（参数不同即视为新权限请求）。

## 后果

- 同一授权在 10 分钟内不重复询问；超时或会话结束自动失效。
- 权限判定在工具实际执行前进行；授权状态改变（降级、参数变更、TTL 过期）后下一次调用按新策略执行。
