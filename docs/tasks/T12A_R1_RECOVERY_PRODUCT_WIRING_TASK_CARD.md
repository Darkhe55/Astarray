# T12A-R1：恢复入口与真实任务续接

> 状态：pending
> 创建日期：2026-09-10
> 类型：核心返修；高风险工作按检查点执行
> 来源：用户授权布置；本文件为Agent派生实施方案，运行态节点默认层级1或以下，不冒充用户层级0
> 前驱：T07D-R1；最终联测需T07D-R2-02及T09A-R1

## 执行契约

必须先读取 [本批实施顺序与共同验收规则](./PRODUCT_INTEGRATION_ROLLOUT.md) 及 AGENTS.md 指定的四份治理文档。该共同文件是本卡的一部分，包含批次、测试、证据、安装、Git及停止条件。原任务卡的未满足验收要求继续有效。

## 目标与范围

让 recover/resume 调用现有恢复控制器并真正恢复可恢复任务；公开查询、用户裁决、调度和资源收口形成闭环。

## 检查点

### T12A-R1-01：恢复命令到应用服务

- 状态：done（2026-09-10；证据 docs/reports/T12A_R1_01_RECOVERY_CENTER_CLI.md）。
- 工作：核对recover相关命令和公开SDK的实际调用链；接通列表、查询、恢复和放弃调度；冻结跨进程身份及会话恢复语义。
- 验收：状态反映磁盘/日志/租约；损坏状态不静默重建成功；abandon保留产物，resume不是固定文案或纯分类结果。
- 前驱：本卡前驱。先通过前驱，再执行本节点。

### T12A-R1-02：副作用与权限对账

- 状态：done（2026-09-10；证据 docs/reports/T12A_R1_02_RECONCILIATION_EVIDENCE.md）。
- 工作：对运行中Provider、工具执行后未确认、用户授权等待、反馈ack前、Git提交后合并前注入进程中断；重启先只读对账。
- 验收：未知副作用blocked，不二次执行；旧一次授权与临时提升不自动延续；原任务历史保持，接手Agent新身份有明确handoff。
- 前驱：T12A-R1-01。先通过前驱，再执行本节点。

### T12A-R1-03：上下文事务恢复

- 状态：done（2026-09-10；证据 docs/reports/T12A_R1_03_CONTEXT_TRANSACTION_EVIDENCE.md）。
- 工作：联测关闭胶囊、全局记录、延后片段、人工签字与补充核验任务的部分提交；验证缓存失效、预算和图revision保持。
- 验收：签字不跨revision复用；补充核验不重复、不丢失；调用/读取预算不因重启清零；复放结果一致。
- 前驱：T12A-R1-02。先通过前驱，再执行本节点。

### T12A-R1-04：安装包恢复与进程收口

- 状态：pending。
- 工作：从tarball启动、在指定故障点中断、另一进程恢复并查询最终结果；验证退出/取消、租约接管和独立反馈进程收口。
- 验收：至少一条恢复任务真正完成产物；不确定任务有可操作裁决；无孤儿进程、双调度或未确认写入。
- 前驱：T12A-R1-03。先通过前驱，再执行本节点。

## 注意事项

pn是反馈单次退避上限，不是三小时自动授权或数据清理期限；等待不得持锁。自动测试使用fake clock和本地服务器。

每轮仅一个检查点；实现、测试、文档与状态证据一起交付。若需要超过三小时，在可构建边界继续拆分。本卡done要求所有必选检查点通过；缺真实服务、人工或平台证据时按明确范围保留blocked/pending，不用说明文字覆盖未满足门禁。

## 验收记录

- 当前提交/工作树基线：`eedc2db`（T09A-R1-04 收口）。用户并行改动（IMPLEMENTATION_PLAN.md、PLAN_STATUS.md、docs/tasks/README.md 及 6 张未跟踪卡）保持未暂存。
- 本检查点实现与入口证据：docs/reports/T12A_R1_01_RECOVERY_CENTER_EVIDENCE.md（§3 dist 端到端命令与退出码、§4 行为反例、§5 门禁结果）。
- 测试命令、退出码：`npm run check` exit 0（166 文件/1450 用例）；`npm run test:coverage` exit 0（全局 branch 86.96%，≥85%）；聚焦 `recover-center-commands` 7/7、`t12a06-cli-wiring` 9/9；dist E2E exit 0/0/0/1/0。
- 人工/外部依赖及剩余风险：`readonly-reconciliation-service` 对账已在 T12A-R1-02 接线完成；检查点生产端记录 `gitStateRecovery`、恢复后任务重新派发与真实 Provider/tarball 重启证据属 T12A-R1-03/-04 与 E2E-01。无人工阻塞项。
- 本地提交、推送尝试与结果：T12A-R1-01 实现提交 `7582dad`、补记 `dcb0288`；T12A-R1-02 实现提交 `daf7cd1`、补记 `d59cc74`；T12A-R1-03 实现提交 `e826e12`（`git push` 第 1 次成功，`8465952..e826e12`）。三次阶段推送均一次成功。

## 首轮执行指令

读取共同实施规则与本卡，核对前驱动态证据。本轮只执行 T12A-R1-01；先记录基线和失败场景，再完成该检查点。不要领取后继，未满足条件不得标记done。

