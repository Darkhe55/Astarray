# 底层架构（Core）

本目录承载与具体界面无关的领域模型、权限、Agent 编排、运行时、工具层、基础设施和独立反馈进程。

依赖规则：

- 可以依赖 Node.js 与无界面的第三方库。
- 不得依赖 `packages/tui`、`packages/gui`、React 或 Ink。
- 权限与备份策略必须在工具实际执行前由本层强制实施，界面提示不能代替底层检查。
- 反馈系统必须继续以独立进程运行，并保留消息原始来源和具体 `agentInstanceId`。

对应测试位于 `tests/core/`。
