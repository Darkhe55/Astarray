# GUI 界面层（预留）

当前版本尚未实现 GUI。本目录用于固定未来 GUI 的边界，避免把 GUI 文件混入底层架构或 TUI。

后续实现约束：

- GUI 只能调用 `packages/core` 的稳定应用接口，不复制领域规则。
- GUI 不得依赖 `packages/tui` 或 Ink；TUI 也不得依赖 GUI。
- 反馈来源、具体 Agent 个体、权限暂停、备份风险警告和高优先级审计必须与 TUI 使用同一核心契约。
- GUI 测试应放入 `tests/gui/`，并独立覆盖组件、集成和可访问性。

本目录没有进入当前构建入口，也不会向用户宣称已交付 GUI。
